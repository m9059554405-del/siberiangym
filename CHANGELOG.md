# Changelog

Все значимые изменения проекта фиксируются в этом файле.
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

## [0.31.0] — 2026-09-17

Панель управления точками сети для CEO по заявке клуба (вне пунктов
`prompt-dorabotki-dlya-kluba.md`): правка и удаление точек, назначение
тренеров и администраторов на точки, мультивыбор площадок в отчётах.
Ни один пункт плана этим релизом не закрывается — метки `[P#.ц]` нет.

### Добавлено
- **Страница «Точки сети»** (`/ceo/gyms`, `web/src/features/ceo/GymsPage.tsx`, новый таб CEO-кабинета): карточки точек со счётчиками (клиенты/тренеры/админы/выручка из уже существующих сетевых фидов P1.7), создание, переименование и правка `selfTrainingMinAge` (`PATCH /gyms/:id`), удаление (`DELETE /gyms/:id` — только пустая точка: отказ при удалении активной точки токена, последней точки сети и любой непустой с перечнем занятых счётчиков; цены и `trainerGyms` удаляются в одной транзакции), переключение активной точки.
- **Тренеры на точках** (та же страница, на эндпоинтах P1.3 `assignToGym`/`unassignFromGym`): назначение/снятие чекбоксами на одну, несколько или сразу все точки; домашняя точка тренера не переносится — только дополнительные, как и в самом P1.3.
- **Администраторы сети**: `GET /gyms/staff` — логины STAFF по всей сети (раньше списка админов не существовало вовсе, у STAFF нет карточки-сущности); `PATCH /gyms/staff/:userId` — перенос админа между точками (только STAFF, обе точки своей сети); `POST /auth/staff` с опциональным `gymId` — создание админа сразу в выбранную точку с валидацией принадлежности сети владельца. Модалка `AddStaffModal` вынесена из `TrainerLoadPage.tsx` в общий `web/src/features/ceo/AddStaffModal.tsx` и получила селектор точки.
- **Мультивыбор площадок в отчётах** (`NetworkGymFilter.tsx`): чекбоксы «вся сеть / одна / группа» вместо одиночного `<select>`.

### Исправлено
- **Отчёты CEO не реагировали на переключатель точки в шапке** (жалоба клуба: «выбрал Просторы — посещаемость не изменилась»): страницы Посещаемость/Выручка/Загрузка тренеров держали собственный фильтр с дефолтом «вся сеть». Теперь дефолт фильтра — активная точка из токена (`defaultGymScope()`, после `switch-gym` страница перезагружается и сразу показывает выбранную точку); блок «Выручка по точкам» показывается при выборе всей сети.

### Не покрыто этим пунктом (явно)
- **Визуальный прогон новой страницы в браузере** — за пользователем: CDP-скриншоты авторизованных страниц по-прежнему запрещены политикой (`RemoteDebuggingAllowed=0x0`, см. 0.30.0).

### Проверено
- **Сборки**: `npm run build` в `api` и `web` — чисто (в `api` — после удаления устаревшего `tsconfig.build.tsbuildinfo`: инкрементальная ловушка nest build, `dist` оставался пустым при «успешной» сборке).
- **Смоук API от лица CEO** на локальной базе: создание точки (`selfTrainingMinAge` 16) → переименование → удаление активной точки (409 «сначала переключитесь») → перенос админа на тестовую точку и обратно → удаление непустой точки (409 с перечнем: пользователи) → создание админа с `gymId` → удаление пустой точки (200). Тестовые данные вычищены (smoke-пользователь удалён из БД), в сети остались 2 точки.

## [0.30.0] — 2026-09-17

UX-фиксы кабинета CEO по итогам ручного визуального прогона (вне пунктов
`prompt-dorabotki-dlya-kluba.md`) плюс инструмент демо-данных для локальной
базы. Ни один пункт плана этим релизом не закрывается — метки `[P#.ц]` нет.

### Исправлено
- **Модалки рендерятся в `document.body`** (`web/src/components/ui/Modal.tsx`, `createPortal`): внутри лэйаута кабинета модалка могла оказаться под fixed-хедером с `backdrop-blur` и визуально «уезжать». Кликом по фону модалка больше не закрывается — только крестиком или явным действием; случайный клик мимо формы перестаёт терять введённые данные.
- **Кнопки «Отмена» / «Готово»** в трёх модалках кабинета: создание логина STAFF (`AddStaffModal`), добавление тренера (`AddTrainerModal`, `TrainerLoadPage.tsx`), создание точки сети (`GymSwitcher.tsx`). «Отмена» — `variant="secondary"`, сбрасывает форму и закрывает; «Готово» появляется в done-состоянии вместо одиночного крестика.
- **Tooltip бар-чарта занятости тренеров** (`TrainerLoadPage.tsx`, `web/src/index.css`): `.recharts-wrapper { position: relative; }` + `isAnimationActive={false}` на Tooltip и всех трёх `<Bar>` — анимация и отсутствие позиционирования контейнера смещали/обрезали всплывашку при наведении.
- **Кнопка «+» переключателя точек** (`GymSwitcher.tsx`) получила `title`/`aria-label` «Добавить точку сети».

### Добавлено
- **`api/prisma/demo-data.ts`** (`npx ts-node prisma/demo-data.ts` из `api/`) — идемпотентный демо-сид локальной базы: начисто вычищает бизнес-данные зала (клиенты/тренеры/абонементы/транзакции/склад/расписания/логи), сохраняя Network, Gym, пользователей CEO/STAFF, цены и упражнения; создаёт 7 тренеров, 44 клиента (PERSONAL/GROUP/SELF), 40 абонементов, 12 товаров + 24 партии склада, 93 транзакции (включая возврат), 123 групповых занятия (−35…+7 дней), 316 персональных слотов во всех статусах, 376 логов тренировок за 30 дней. Предназначен для демонстраций и локальной разработки, не для прода.

### Не покрыто этим пунктом (явно)
- **Визуальная проверка авторизованных страниц автоматикой не выполнена**: корпоративная политика `RemoteDebuggingAllowed=0x0` (Chrome и Edge) блокирует CDP-отладку, скриншот-прогон невозможен; headless-скриншотом проверен только публичный экран логина. Зафиксировано как ограничение среды.
- **Демо-данные касаются только локальной базы** SiberianGym; прогон `demo-data.ts` на проде не предусматривался и не проверялся.

### Проверено
- **Web:** `npm run build` (`tsc -b` + vite + PWA) — чисто. **API:** код не менялся (`demo-data.ts` — standalone ts-node-скрипт вне nest-сборки).
- Сид прогнан на локальной базе; сверка через API от лица CEO: 44 клиента, 7 тренеров, 93 транзакции на 374 140 ₽.

## [0.29.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.9]**,
миграция существующих прод-данных при введении сети. SQL-бэкфилл внутри
миграции P1.1 уже создаёт Network и привязывает единственный Gym, но
документ требует большего: явный, прогнанный на копии прод-базы
migration-скрипт с видимым отчётом, а не «молча» выполненный SQL в общем
потоке миграций.

### Добавлено
- **[P1.9] `api/prisma/network-migration.ts`** (`npm run prisma:network-migration`) — явный отчёт-проверка, прогоняемый на копии прод-базы ПОСЛЕ `prisma migrate deploy` и ДО выкладки на прод (порядок — в шапке скрипта). Без флагов read-only и идемпотентен: сети/залы/владельцы (роль CEO обязательна — `Network.ownerId` намеренно без FK, осиротевшая ссылка — главный риск), число строк users/clients/trainers/transactions/orders по каждому залу («данные не потеряли связь»), ноль осиротевших строк по 7 gym-скоуп таблицам (gym_id в никуда), наличие `MembershipPricing` на каждой точке (без него продажа абонементов невозможна, P1.2). Код выхода 1 при проблемах — чтобы прогон вставить в чек-лист релиза.
- **[P1.9] `--set-owner <email>` / `--name <название>`** — починка владельца сети (только существующий пользователь с ролью CEO, иначе отказ) и переименование сети; флаги работают при ровно одной сети (прод-сценарий P1.9).
- **[P1.9] Репетиция прогона на «легаси-проде».** На локальном Postgres создана scratch-БД: init-схема + синтетические данные ДО-P1.1 (зал, CEO+STAFF, клиент, тренер, транзакция, цены), затем `prisma migrate deploy` прогнал все 14 миграций, включая бэкфилл P1.1 на этих строках. Проверены все ветки скрипта: чистый прогон (exit 0); осиротевший владелец (`owner_id` → ghost) — проблема найдена, exit 1; `--set-owner` для не-CEO — отказ, exit 1; `--set-owner` настоящим CEO — починено, exit 0; повторная проверка — exit 0. Scratch-БД удалена после прогона.

### Не покрыто этим пунктом (явно)
- **Прогон на РЕАЛЬНОМ дампе прод-базы** — репетиция сделана на синтетических легаси-данных локального Postgres; сам дамп с этой машины недоступен. Перед выкладкой на прод — восстановить дамп в отдельную базу и пройти шаги из шапки скрипта (`deploy → скрипт → при необходимости --set-owner`).
- **Плана релиза с окном обслуживания нет** — скрипт и миграции не блокируют работу пользователей (read-only проверки + одиночный UPDATE), но формального даунтайм-плана документ не содержит и это не код.
- **Откат миграций не предусмотрен** — Prisma не поддерживает down-миграции; страховка — сам дамп, из которого восстанавливалась копия.

### Проверено
- Полная репетиция на scratch-БД локального Postgres (все ветки выше). `npm run build` (nest build, api) — чисто; изменений веба нет (`web/package.json` поднят только для синхронности версии релиза).

## [0.28.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.7]**,
отчётность CEO по сети. Все три отчёта кабинета (выручка, посещаемость,
занятость тренеров) строились на фидах, жёстко ограниченных `gymId` из
токена: сводка по сети требовала раздельного захода в каждую точку через
`switchGym` (P1.1), агрегации «одним экраном» не существовало.

### Добавлено
- **[P1.7] `GymsService.resolveNetworkGymIds(actor)`** — единая точка входа для сетевого охвата: все точки сети, к которой относится текущая точка токена (та же логика принадлежности, что P1.1–P1.3/P1.5). Роль проверяют вызывающие — сводка по сети только для CEO; STAFF остаётся строго в своей точке (P1.6).
- **[P1.7] Выручка по всей сети + разбивка по точкам.** `GET /transactions` (эндпоинт и так CEO-only) отдаёт транзакции всех точек сети, к каждой строке приложена точка (`gymId` + имя). На «Выручке» — фильтр «Вся сеть / конкретная точка» (мгновенный, на клиенте: данные уже сетевые) и сводка «Выручка по точкам» одной страницей. Топ тренеров по выручке строится теперь из самих транзакций (имя/аватар приложенного тренера), поэтому корректен по всей сети — раньше имена resolving'ом брались из тренеров одной точки.
- **[P1.7] Посещаемость по сети.** `GET /workout-logs` для CEO — логи всех точек (точка клиента приходит внутри `client.gymId`, по ней фильтр); `GET /group-classes` и `GET /personal-slots` для CEO — расписание сети целиком (`ScheduleService.gymIdsForActor`), клиент и STAFF видят только свою точку, как раньше.
- **[P1.7] Занятость тренеров по сети.** `GET /trainers` для CEO — тренеры всех точек сети (домашняя ИЛИ дополнительная, `scopeToGyms`); к карточкам приложены `additionalGyms` — основа клиентского фильтра «тренер работает на выбранной точке». Карточка тренера (`GET /trainers/:id`) получила тот же охват — иначе из сетевого списка она открывалась бы 404. `createLogin` тренеру чужой точки заводит логин в ДОМАШНЕЙ точке тренера, а не в той, где сейчас находится CEO. Подопечные — `GET /clients?network=1` (флаг учитывается только для CEO): список только для чтения под отчёт, все поштучные операции с клиентами остались gym-scoped; `isMinor` в сетевом списке считается по порогу самостоятельных тренировок точки каждого клиента.
- **[P1.7] `NetworkGymFilter`** (`web/src/features/ceo/NetworkGymFilter.tsx`) — общий селектор «Вся сеть / точка» для трёх отчётных страниц; не показывается вне роли CEO и в однокточечной сети.

### Не покрыто этим пунктом (явно)
- **Сетевой стала только отчётность.** Точечные операции CEO (управление клиентами/карточкой, склад, заказы, расписание-creation) по-прежнему работают от токена конкретной точки через `switchGym` (P1.1); сетевой список клиентов — read-only.
- **Нет серверной агрегации и пагинации** — каждый фид отдаёт всю сеть одним ответом, сводки считаются на клиенте; для сетей из десятков точек понадобится серверная агрегация, на текущих размерах это избыточно.
- **Журнал активности, офферы, директор-сообщения** в кабинете CEO остались по точке токена — в тексте P1.7 перечислены только выручка/посещаемость/занятость.
- Сквозной live-сценарий на двух точках в этой сессии не прогонялся (проверка сборкой/типами).

### Проверено
- **Backend:** `npm run build` (nest build) — чисто. **Web:** `npm run build` (`tsc -b` + vite + PWA) — чисто. `oxlint` в этом окружении не запускается (сломанный native binding npm, проблема среды, не кода).

## [0.27.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.6]**,
роли по точкам vs роли по сети. Аудит всех эндпоинтов, отдающих данные
конкретного клиента по id из запроса, подтвердил: почти всё уже ограничено
своей точкой (`gymId` из токена), но оставались дыры ровно того класса,
которого боится документ — «чужая площадка через подбор ID» и «доступ
тренера по инерции старой связи».

### Добавлено
- **[P1.6] Реальная дыра закрыта: замеры и фото прогресса.** `GET /clients/:clientId/measurements` и `GET /clients/:clientId/progress-photos` раньше отдавали чувствительные данные (биоимпеданс, «было/стало») по одному только id — без проверки ни точки, ни связи с клиентом: токен CEO/STAFF/TRAINER любой точки мог читать их про любого клиента клуба вообще. Теперь: CEO/STAFF — только клиент своей точки, TRAINER — только свой подопечный.
- **[P1.6] `assertCanViewClientData`** (`api/src/clients/client-access.util.ts`) — единая проверка «своя точка vs чужая точка сети» + «свой подопечный для тренера» для всех данных конкретного клиента. Тот же тип авторизации, что уже чинили в 0.5.0 для self-service доступа клиента к чужой карточке, теперь применён на уровне «точка vs точка сети».
- **[P1.6] Мгновенный отзыв доступа тренера при переназначении.** Workout-логи (`GET /clients/:clientId/workout-logs`) и программа (`GET/PUT /clients/:clientId/program`) теперь тоже требуют `client.trainerId === карточка тренера`, а не просто «тренер этого зала». Все три момента переназначения (заказ TARIFF_CHANGE, возврат тарифа, уход в самостоятельные) обновляют `trainerId` транзакционно, а guard'ы читают его в моменте — доступ старого тренера к текущим логам, замерам, фото и программе закрывается сразу, без инерции старой связи. Список «мои клиенты» (`GET /trainers/me/clients`) и карточка клиента (паттерн 0.5.0, `findOneForActor`) фильтруются тем же полем — весь путь тренера до данных клиента согласован.
- **[P1.6] Аудит остальных STAFF-путей** (склад, шкафчики, заказы/возвраты/транзакции, клининг, оборудование, согласия, законные представители, обратная связь, аутрич, расписание, точки сети) — везде `gymId`-скоуп из токена уже стоял. Единственная найденная щель за пределами клиентских данных — оракул возраста в minor-check заказа абонемента: `birthday` читался по голому id, и различие ошибок выдавало существование/возраст клиента чужой точки; чтение ограничено своей точкой.

### Не покрыто этим пунктом (явно)
- **«CEO видит сеть целиком и фильтрует по точке»** — в части отчётности это P1.7 (сводная агрегация по сети); в части точечных операций CEO по-прежнему работает от токена конкретной точки (`switchGym`, P1.1), guard'ы для CEO/STAFF едины осознанно.
- **Доступ тренера закрыт ко ВСЕМ логам клиента**, включая период своей работы, — документ требовал закрыть текущие; более широкое закрытие проще и безопаснее, история доступна CEO/STAFF точки.
- **Тренер дополнительной точки (P1.3, TrainerGym)** не получает доступ к данным клиентов той точки, кроме назначенных ему, — назначение клиента тренеру сейчас и так ограничено своей точкой, это не менялось.

### Проверено
- `npm run build` (nest build, api) — чисто; изменений веба нет (интерфейс тренера и так заходит к данным только из списка «мои клиенты»).
- Сквозной live-сценарий с двумя точками в этой сессии не прогонялся (проверка сборкой/типами).

## [0.26.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.5]**,
чек-ин на любой точке сети по одному абонементу. До этого администратор
чужой точки сети вообще не имел инструмента узнать гостя: поиск клиентов
жёстко ограничен `gymId` токена, и действительность NETWORK-абонемента
(0.23.0) на своей точке приходилось оценивать «на глаз», без карточки.

### Добавлено
- **[P1.5] Межточечный поиск клиента** — `GET /clients/network-search?q=` (`ClientsService.networkSearch`): по имени (без учёта регистра) или телефону по ВСЕМ точкам сети, которые находит `GymsService.resolveNetworkId` (та же логика принадлежности сети, что в P1.1–P1.3). Доступ — `@Roles(CEO, STAFF)`; роут объявлен ДО `:id`, чтобы не повторять класс баги P0.6 (перехват статических путей параметрическим). Ограничение выдачи — 20 записей.
- **[P1.5] Валидация «покрывает ли абонемент именно эту точку»** — вычисляется на лету в ответе поиска (`validHere`), без новых полей и миграций: абонемент `ACTIVE` **и** (`scope === 'NETWORK'` **или** карточка заведена на текущей точке). Ровно та формулировка из документа: валидируется не «клиент принадлежит этому `gymId`», а «абонемент активен и покрывает именно эту точку сети». GYM-абонемент действует только на домашней точке, NETWORK (0.23.0) — на любой.
- **[P1.5] Секция «Клиент с другой точки сети»** (`web/src/features/staff/ClientsManagePage.tsx`) — показывается только если сеть реально состоит больше чем из одной точки: имя, телефон, домашняя точка (с пометкой «эта точка»), тип абонемента («вся сеть» для NETWORK) и бейдж «Действует здесь / Не действует здесь». Карточка при этом остаётся физически в домашнем зале — пересмотр привязки сущностей сознательно не делается здесь (см. P1.6/P1.8).

### Не покрыто этим пунктом (явно)
- **Это не турникет и не QR** — физический контроль доступа на входе остаётся за P2.2; здесь закрывается именно «проверка на ресепшене глазами администратора», что и требует текст P1.5.
- **Факт посещения нигде не фиксируется** — нет записи визита и списания `visitsLeft` по результату проверки; аудит посещений целиком относится к P2.2.
- **С чужой точки нельзя оформить действия** (запись на занятие, продление, заказ) — поиск даёт «узнать и проверить», карточка и операции остаются за домашней точкой (та же граница, что отмечена в P1.6/P1.8).
- **Пагинации нет** — один жёсткий `take: 20` по релевантности не сортируется (по алфавиту имён); для ресепшена с уточняющим запросом достаточно.

### Проверено
- **Backend:** `npm run build` (nest build) — чисто. **Web:** `npm run build` (`tsc -b` + vite + PWA) — чисто. `oxlint` в этом окружении не запускается (сломанный native binding npm, проблема среды, не кода).
- Сквозной live-сценарий на двух точках в этой сессии не выполнялся (сборки/typescript-проверка только).

## [0.25.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.4]**,
инструмент CEO «Завести нового тренера». Заведение тренера уже существовало
(`POST /trainers` + отдельный `POST /trainers/:id/create-login`), но без
статуса занятости, доли от выручки, привязки к точкам сети при заведении
и согласия сотрудника — а также как два раздельных действия CEO вместо
одного.

### Добавлено
- **[P1.4] Статус занятости и доля от выручки** (`Trainer.employmentType`, `Trainer.revenueSharePercent`; миграция `20260917020526_p1_4_trainer_employment_and_consent`) — оба поля nullable намеренно: у уже существующих (реальных прод-) тренеров этот вопрос никто не задавал, выдумывать значения задним числом неверно; для новых тренеров, заводимых через форму, `employmentType` обязателен. Карточка тренера у CEO теперь показывает не только «выручку по тренерам» (0.10.0), но и расчёт «сколько причитается самому тренеру» — именно то, чего прямо не хватало по тексту задачи.
- **[P1.4] Согласие тренера как сотрудника (`STAFF_PDN`)** — текст и тип уже существовали с P0.4, но фактически никогда не могли быть записаны: `ConsentRecord.clientId` был обязательным полем, рассчитанным только на клиентов. Ослаблен до nullable, добавлен параллельный nullable `trainerId` — ровно один из двух всегда заполнен. Оформляется тем же принципом, что согласие законного представителя (P0.6): CEO/STAFF фиксирует по факту подписанной на месте бумаги, не сам тренер через self-service (тем более что при заведении «в один проход» у него может ещё не быть логина).
- **[P1.4] Один инструмент вместо двух отдельных действий.** `POST /trainers` теперь одним вызовом создаёт карточку, назначает на дополнительные точки сети (P1.3), фиксирует согласие сотрудника и сразу выдаёт логин — если соответствующие поля переданы. Старое двухшаговое поведение (создать → отдельно `create-login`) продолжает работать как самостоятельный эндпоинт, ничего не сломано, просто теперь не единственный путь.
- **[P1.4] Назначение на несколько точек — только CEO, как явно требует документ.** STAFF по-прежнему заводит тренера (`@Roles(CEO, STAFF)` не менялся), но попытка передать `additionalGymIds` от лица STAFF отклоняется (`ForbiddenException`) — «если STAFF тоже должен уметь заводить тренеров, то только в рамках своей точки». Форма на фронте физически не показывает мультивыбор точек не-CEO.

### Не покрыто этим пунктом (явно)
- **Расчёт «причитается тренеру» — это просто отображаемая арифметика** (выручка × процент), не полноценный модуль расчёта и истории выплат — нет фиксации факта выплаты, нет истории по периодам. Это, по сути, следующий логический шаг, не заявленный явно в тексте этого пункта.
- **Специализация/направления не изменены** — уже была свободной строкой с 0.5.0, документ отдельно подтверждает, что это уже работает; менять было нечего.
- **Отзыв согласия сотрудника не реализован** — только фиксация (granted: true), как и для законного представителя в P0.6; симметрично тому же решению.

### Проверено
- **Backend:** `npx tsc --noEmit -p tsconfig.build.json` — чисто.
- **В браузере, полный сквозной сценарий CEO:** «Новый тренер» → заполнены статус занятости (самозанятый), 40% от выручки, отмечено согласие сотрудника, сразу указаны email/пароль → создан один действием (`POST /trainers` с полным набором полей, без отдельного `create-login`) → карточка тренера показывает «Самозанятый», «Причитается тренеру — 0 ₽ / 40% от выручки», «Согласие сотрудника — Подписано» → выход и вход под выданным логином тренера подтверждён (роль TRAINER, попал в свой кабинет).
- Тестовый тренер (с логином, согласием) удалён из БД после проверки.

## [0.24.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.3]**,
тренер на нескольких точках сети. Личное расписание тренера было жёстко
привязано к одному залу — с ростом сети тренер, ведущий занятия на двух
площадках, физически не мог быть заведён на обеих одновременно.

### Добавлено
- **[P1.3] `TrainerGym`** (`api/prisma/schema.prisma`, миграция `20260917015346_p1_3_trainer_multi_gym`) — многие-ко-многим сверх домашней точки тренера (`Trainer.gymId` остаётся местом, где заведена карточка и логин). Тренер без единой доп. записи работает ровно как раньше — это чистое расширение, не переделка существующей модели.
- **[P1.3] Тренер виден и назначаем на любой из своих точек.** `TrainersService.findAll`/`findOne` теперь матчат тренера и по домашнему `gymId`, и по `TrainerGym` — тренер, назначенный на вторую точку, появляется в списке тренеров той точки наравне с местными. `schedule.service.ts` (`createGroupClass`/`createPersonalSlot`) проверяет тренера через ту же логику (`assertTrainerAtGym`), а не только «тренер этого зала» — CEO/STAFF второй точки может создать занятие с приезжающим тренером.
- **[P1.3] Календарь тренера — вся сеть без переключения контекста**, ровно как требует пункт. `GET /group-classes` и `GET /personal-slots` для роли TRAINER возвращают записи со ВСЕХ точек, на которых он работает (домашняя + `TrainerGym`), одним списком — без параметра, без переключения точки. Для остальных ролей (клиент, STAFF, CEO) поведение не изменилось: строго текущая точка по токену — они управляют конкретной точкой, а не всей сетью разом.
- **[P1.3] Инструмент CEO «Назначить тренера на точку»** (`web/src/features/ceo/TrainerDetailPage.tsx`, секция «Точки сети») — список уже назначенных точек (домашняя помечена отдельно, снять её нельзя — только дополнительные) и выпадающий список остальных точек своей сети для назначения. `POST/GET /trainers/:id/gyms`, `POST /trainers/:id/gyms/:gymId/unassign` — доступны только CEO, принадлежность точки своей сети проверяется тем же `GymsService.assertBelongsToOwnedNetwork`, что и в P1.1/P1.2, не параметром запроса.

### Не покрыто этим пунктом (явно)
- **Персональный тренировочный тариф/цена личной тренировки одна на тренера**, не различается по точке — если тренер ведёт на двух площадках с разной ценовой политикой, `personalSessionPrice` придётся либо унифицировать, либо решать отдельно (не заявлено в этом пункте документа).
- **Нет UI-предупреждения о пересечении расписания тренера между точками** — тренер физически не может вести занятие одновременно в двух местах, но создание слота на точке B не проверяет, что в это же время у него уже есть занятие на точке A. Это тот же класс проблемы, что и общая P2.7 (защита от двойного бронирования) — сознательно не решается здесь, чтобы не дублировать её отдельной локальной проверкой.
- **Расписание/тарифы сопровождения клиента (Tariff) по-прежнему не знают о точках** — назначение тренера клиенту (`TARIFF_CHANGE`) не проверяет, что тренер и клиент физически на одной точке в моменте (тренер мультиточечный, клиент — нет, пока не сделан P1.2 полностью на уровне клиента, что явно не входит в P1.3).

### Проверено
- **Backend:** `npx tsc --noEmit -p tsconfig.build.json` — чисто.
- **Полный сквозной сценарий через API (curl):** создана вторая точка сети → создан тестовый тренер на домашней точке → назначен на вторую точку через `POST /trainers/:id/gyms` → подтверждено: тренер появляется в `GET /trainers` при токене, скопированном на вторую точку → персональный слот создан на второй точке от лица CEO, скопированного туда, → подтверждено `gymId` слота = вторая точка → CEO на ДОМАШНЕЙ точке не видит этот слот в своём `GET /personal-slots` (точечная изоляция для не-тренера сохранена) → выдан логин тренеру → от лица самого тренера `GET /personal-slots` вернул слоты СРАЗУ с обеих точек одним списком, без параметра переключения.
- **В браузере:** секция «Точки сети» в карточке тренера CEO-кабинета отражает назначение сразу после `POST`, кнопка «Снять» показана только у дополнительных точек (не у домашней).
- Тестовый тренер (с логином), тестовые персональные слоты и тестовая вторая точка сети удалены из БД после проверки.

## [0.23.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.2]**,
абонемент на уровне сети. Без этого пункта клиент, купивший абонемент на
одной точке, физически не смог бы воспользоваться им на второй — а это и
есть весь смысл сети для гостя. Решена развилка, которую документ прямо
просил не оставлять неявной: точка может продавать обычный тариф («эта
точка») и/или более дорогой сетевой («вся сеть») параллельно, а не выбирать
между ними на уровне всей системы.

### Добавлено
- **[P1.2] `MembershipScope` (`SINGLE_GYM` | `NETWORK`)** — новое поле на `Membership` (миграция `20260917004816_p1_2_network_membership_scope`), фиксирует, что именно купил клиент. Сетевые цены — 4 новых nullable-поля на `MembershipPricing` (`singleNetwork`/`monthlyNetwork`/`pack10Network`/`pack20Network`): `null` означает, что конкретная точка не продаёт сетевой вариант этого типа абонемента вообще, поэтому не каждая точка сети обязана предлагать оба варианта.
- **[P1.2] `orders.service.ts`** — `MEMBERSHIP_PURCHASE`/`MEMBERSHIP_RENEWAL` принимают `meta.scope` (по умолчанию `SINGLE_GYM` — обратная совместимость со всеми заказами до появления сети), цена считается из правильного столбца `MembershipPricing`, попытка купить `NETWORK` без настроенной сетевой цены на этой точке отклоняется понятной ошибкой. `scope` попадает в снимок `_prevState`, поэтому возврат (P0.7) корректно откатывает абонемент к прежней области действия, а не только к типу.
- **[P1.2] Раньше цены абонементов нельзя было изменить нигде, кроме прямого запроса к БД** — ни одного эндпоинта не существовало вообще (только seed-скрипт при первом развёртывании). Добавлен `PATCH /pricing` (только CEO, частичное обновление) — без этого сетевые цены из этого же пункта было бы физически нечем задать через приложение. Раздел «Тарифы абонементов» с инлайн-редактированием добавлен в CEO-кабинет («Клиентская база»).
- **[P1.2] Покупка/продление с выбором области действия.** Клиент (`PaymentsPage`) видит для каждого типа абонемента отдельную кнопку «Купить» на сетевой вариант, если точка его продаёт — рядом с обычной ценой, а не вместо неё. STAFF/CEO (карточка клиента) — тот же выбор чекбоксом «Оформить на всю сеть», появляется только когда сетевая цена для выбранного типа настроена. Текущий абонемент клиента везде, где показывается (шапка личного кабинета, карточка в `ClientsManagePage`), помечен «· вся сеть», если это так.
- **[P1.2] Свежедобавленная точка сети не остаётся без цен.** `GymsService.create()` (P1.1) копирует текущий прайс-лист CEO (включая уже настроенные сетевые цены) на новую точку как стартовый набор — иначе после `POST /gyms` точка физически не могла бы продать ни одного абонемента (`PricingService.get` кидает 404, пока строка `MembershipPricing` не создана), а взять её было бы неоткуда, кроме ручного скрипта.

### Не покрыто этим пунктом (явно) — физическая проверка на месте, это отдельные пункты
- **Ничего не проверяет право клиента физически зайти на конкретную точку** — эта версия только про то, что купить и как это учитывается в данных. Реальный контроль на входе (турникет/QR у администратора) — P2.2/P1.5, которых ещё нет вообще ни в каком виде; когда они появятся, они должны будут явно смотреть на `Membership.scope`, а не только на `status`.
- **Нет способа посмотреть/управлять клиентом сети с чужой точки.** Если у клиента `NETWORK`-абонемент и он пришёл на вторую точку, администратор той точки пока не может найти его карточку иначе как узнав `gymId`, к которому она привязана (`Client` по-прежнему принадлежит ровно одному `gymId` — межточечный поиск клиента явно вынесен в P1.5/P1.6, не решается здесь).
- **Персональные/групповые тренировки не участвуют в сетевом тарифе** — только 4 базовых типа абонемента (`SINGLE`/`MONTHLY`/`PACK10`/`PACK20`). Осознанное решение: персональная тренировка с конкретным тренером на конкретной точке не имеет естественного «сетевого» варианта (P1.3, тренер на нескольких точках, ещё не сделан).

### Проверено
- **Backend:** `npx tsc --noEmit -p tsconfig.build.json` — чисто.
- **В браузере, полный сквозной сценарий:** CEO настроил сетевую цену на месячный абонемент (6000 ₽ при обычной цене 4500 ₽) через новую секцию «Тарифы абонементов» → STAFF оформил тестовому клиенту продление с отмеченным «Оформить на всю сеть» → заказ создан на правильную сумму 6000 ₽ (не 4500) → чек подтверждён (API, эмуляция QR) → `Membership.scope` в БД подтверждён как `NETWORK` → карточка клиента у STAFF показывает «вся сеть» → личный кабинет самого клиента показывает «· вся сеть» в шапке и «Активен» именно у сетевого варианта карточки абонемента, а не у обычного.
- Тестовый клиент, временный STAFF-аккаунт и тестовая сетевая цена удалены/сброшены после проверки.

## [0.22.0] — 2026-09-17

Продолжение Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.10]**,
инструмент CEO «Завести администратора» и связанное с ним решение по смене
ролей. Документ прямо отмечает не дожидаться остального Приоритета 1 —
базовая многопользовательская работа нужна независимо от готовности сети,
и это подтвердилось на практике в этой же сессии: единственный способ
завести тестовый STAFF-аккаунт при проверке P0.6 был — написать
отдельный Node-скрипт напрямую в базу, потому что в приложении для этого
не было вообще никакого пути.

### Добавлено
- **[P1.10] `POST /auth/staff`** (CEO) заменил собой непроверенный и не подключённый ни к одному интерфейсу `POST /auth/users` (упоминался в 0.3.0 только в контексте утечки `passwordHash`, реально нигде не вызывался — проверено `grep` по всему `web/src`). Новый эндпоинт заводит STAFF одним действием, сразу в ту точку сети, где активен токен CEO — в отличие от тренера (P1.4, ещё не сделан), у STAFF нет отдельной карточки-сущности, второй шаг не нужен.
- **[P1.10] Пересмотренное решение, зафиксировано явно.** Старый `CreateUserDto` принимал `role` любым значением enum, включая `CEO` — то есть теоретически (хоть и без интерфейса) CEO мог создать через API ещё один аккаунт с ролью CEO. Это не просто нежелательно, а стало явно некорректным после P1.1: у нового CEO-пользователя не было бы своей `Network` (`Network.ownerId` уникален), и он ловил бы `ForbiddenException` на каждом сетевом эндпоинте. Новый `CreateStaffDto` вообще не принимает `role` — всегда `STAFF`; CLIENT/TRAINER по-прежнему заводятся только через свои профильные модули, где сразу создаётся и бизнес-карточка.
- **[P1.10] `User.name`** (миграция `20260917002507_p1_10_user_name`) — раньше сотрудник с ролью STAFF в журнале изменений был неотличим от любого другого STAFF: `resolveActorName` возвращал жёстко строку `«Администратор»` на всех. Теперь ФИО указывается при заведении и видно в каждой записи журнала как имя конкретного человека.
- **[P1.10] Явное решение по смене роли, а не оставлено неявным, как просил документ.** Роль после создания учётной записи неизменяема в этой версии — нет `PATCH /users/:id/role`. Причина: аудируемая смена роли осмысленна только вместе с деактивацией старой роли (иначе можно тихо получить доступ на несколько ролей сразу), а сама деактивация (P3.9) ещё не реализована — добавлять один без другого means либо непроверенный код, либо функциональность с дырой. Единственный сегодняшний путь сменить роль сотруднику — завести новую учётную запись, старую не отзывать (тоже ждёт P3.9).

### Исправлено (найдено при подготовке этого пункта — регрессия из P1.1, не из более ранней версии)
- **Бутстрап на пустой БД был сломан для только что выпущенного P1.1.** `Gym.networkId` стал обязательным полем в 0.21.0, а `prisma/seed.ts` по-прежнему создавал `Gym` без него — первое разворачивание системы (`prisma migrate deploy && prisma db seed`, ровно то, что описывает `DEPLOY_GUIDE.md`) упало бы сразу на первом же шаге. Причина глубже опечатки: `Gym` требует существующую `Network`, `Network` в обычном варианте требовала бы существующего владельца-CEO (`User`), а `User` требует существующий `Gym` — настоящая циклическая зависимость трёх сущностей при самом первом бутстрапе, а не что-то, что можно было решить только порядком операций.
- **Починка:** `Network.ownerId` перестал быть внешним ключом на уровне БД (обычным полем с уникальным индексом остался) — это единственная точка цикла, где реальной необходимости в FK нет: удаления `User` в системе не существует вообще (P3.9 — деактивация, не удаление), а все сетевые эндпоинты (`resolveOwnedNetworkId`) и так явно проверяют владельца при каждом обращении, не полагаясь на ограничение БД. Это разрывает цикл: `seed.ts` теперь создаёт `Network` (с заранее сгенерированным `id` будущего CEO в качестве `ownerId`) → `Gym` (ссылается на готовую `Network`) → `User` (с тем самым заранее сгенерированным `id`, ссылается на готовый `Gym`) — три обычных, непротиворечивых вставки по порядку, без вставки в базу «сначала, чинить потом».
- Проверено прогоном на пустой БД: `prisma migrate deploy` (все 11 миграций, включая эту) и `prisma db seed` на только что созданной, ранее нетронутой БД — оба шага завершились без ошибок, `Network`/`Gym`/`User` (CEO) созданы с корректными перекрёстными ссылками; повторный запуск `seed` корректно идемпотентен (не создаёт дублей).

### Не покрыто этим пунктом (явно)
- **Форма создания STAFF не даёт выбрать точку сети** — сотрудник всегда заводится в ту точку, в которой сейчас находится CEO (по активному токену), явного селектора точки нет. Для сети из одной активной точки за раз (типичный сценарий CEO, который просматривает одну точку) это не проблема, но при добавлении P1.6 (роли по точкам) стоит вернуться и добавить явный выбор.
- **Смена и повышение роли** — как решено выше, не реализовано в этой версии, ждёт P3.9.
- **Множественность CEO/совладельцев одной сети** — по-прежнему не решается, ссылается на ту же нерешённую развилку из P1.1/0.21.0.

### Проверено
- **Backend:** `npx tsc --noEmit -p tsconfig.build.json` — чисто.
- **Бутстрап с нуля:** отдельная одноразовая тестовая БД, `prisma migrate deploy` (11 миграций) → `prisma db seed` — оба шага прошли без ошибок, структура данных проверена напрямую (одна `Network`, один `Gym` внутри неё, один `User`-CEO с `id`, совпадающим с `Network.ownerId`); повторный запуск `seed` — идемпотентен. Тестовая БД удалена после проверки.
- **В браузере, сквозной сценарий CEO → STAFF → снова CEO:** вход CEO → кнопка «Новый администратор» на «Загрузке тренеров» → заведён тестовый администратор (ФИО, email, телефон, пароль) → выход, вход под новым администратором подтверждён (роль STAFF, попал в раздел «Клиенты») → зарегистрирован тестовый клиент от его имени → вход обратно CEO → «Журнал изменений» показал именно ФИО тестового администратора (не общее «Администратор») напротив записи о регистрации клиента.
- Тестовый администратор и тестовый клиент удалены из БД после проверки.

## [0.21.0] — 2026-09-17

Начало Приоритета 1 (`prompt-dorabotki-dlya-kluba.md`) — **[P1.1]**, сущность
«Сеть» (Network) над Gym. Один CEO может теперь управлять несколькими
залами-площадками как одной сетью, а не только одним залом — фундамент,
на который по документу должны опираться остальные пункты Приоритета 1
(P1.2–P1.10) и часть Приоритета 2.

### Добавлено
- **[P1.1] Сущность `Network`** (`api/prisma/schema.prisma`, миграция `20260917001252_p1_1_network`) — `ownerId` (CEO, поле уникальное: один CEO = одна сеть, множественность совладельцев осознанно не решается здесь, это отдельная развилка в P1.10) и связь `gyms: Gym[]`. `Gym.networkId` теперь обязательное поле — даже единственный сегодняшний зал обязан принадлежать сети.
- **[P1.1] Безопасный бэкфилл существующих прод-данных прямо в самой миграции** (не отдельным скриптом "когда-нибудь потом", как предполагал P1.9 для будущего) — для каждого уже существующего `Gym` находит его CEO и создаёт для него `Network`, и только после того как каждая строка получила `network_id`, колонка становится `NOT NULL`. Проверено на реальной структуре текущей БД (один зал SiberianGym, один CEO) — после миграции ровно одна `Network` с этим CEO как владельцем и этим залом внутри.
- **[P1.1] `api/src/gyms/`** — новый модуль, доступен только CEO: `GET /gyms` (все точки своей сети), `POST /gyms` (добавить ещё одну точку в свою сеть). Принадлежность сети определяется не параметром запроса, а тем, в каком `Gym` реально скопирован токен вызывающего CEO (`resolveOwnedNetworkId`) — не даёт подставить чужой `networkId`.
- **[P1.1] Переключение активной точки без повторного входа** (`POST /auth/switch-gym`, CEO) — перевыпускает JWT с тем же `sub`/`role`, но другим `gymId`. Работает "бесплатно" для абсолютно всех существующих эндпоинтов без единого изменения в них: `JwtStrategy.validate` отдаёт payload как есть в `request.user`, и все сервисы везде уже читают `gymId` из токена, а не из базы — значит смена точки является полностью корректной сменой контекста, а не отдельной веткой авторизации, которую пришлось бы поддерживать параллельно.
- **[P1.1] Переключатель точки в шапке приложения** (`web/src/components/GymSwitcher.tsx`, встроен в `RootLayout`, виден только CEO) — выпадающий список точек сети и кнопка добавления новой. После переключения — полная перезагрузка страницы (осознанное решение: весь кэш `react-query` и состояние компонентов сейчас неявно завязаны на «текущий» `gymId` из токена; перезагрузка — самый простой и надёжный способ гарантированно не оставить на экране новой точки ни одного значения, закэшированного от предыдущей).

### Не покрыто этим пунктом (явно) — это отдельные пункты P1.2–P1.10, не пропущенные части P1.1
- **Абонемент клиента работает только в пределах одной точки** (P1.2) — клиент, купивший абонемент на одной точке сети, физически не сможет им воспользоваться на второй, у него будет просто другая, пустая клиентская база. Ни `Client`, ни `MembershipPricing` не научились работать на уровне сети — это отдельный явно нерешённый в документе вопрос (тариф на всю сеть vs тариф на одну точку, разная цена).
- **Тренер не может работать на нескольких точках сети одновременно** (P1.3) — тренер, как и клиент, всё ещё привязан к одному `gymId`.
- **Инструмент CEO «Завести тренера/администратора» не расширен под выбор точки** (P1.4/P1.10) — `POST /trainers`, `POST /auth/users` по-прежнему создают сотрудника в ТЕКУЩЕЙ активной точке (по токену), без явного поля выбора точки при создании.
- **Отчётность CEO (выручка/посещаемость/занятость) — по-прежнему про одну точку зараз** (P1.7), без сводной агрегации по всей сети целиком. Переключатель точки в шапке меняет, какую ОДНУ точку показывает CEO — не показывает сеть суммарно.
- **Удаление/деактивация точки не реализованы** — есть только создание. Тестовая точка, добавленная при проверке этого пункта, удалена напрямую через Prisma-скрипт, не через API (такого эндпоинта нет и не планировался в рамках P1.1).
- **Множественность CEO/совладельцев одной сети** — прямо зафиксировано как отдельный, не решённый здесь вопрос (см. P1.10 в самом документе).

### Проверено
- **Backend:** `npx tsc --noEmit -p tsconfig.build.json` — чисто, без ошибок.
- **Бэкфилл на структуре, максимально близкой к реальной проде:** после применения миграции к текущей dev-базе (один зал SiberianGym, один CEO) создалась ровно одна `Network`, владелец — существующий CEO, единственный зал корректно оказался внутри неё — проверено прямым запросом к БД.
- **В браузере, полный сценарий под ролью CEO:** вход → в шапке появился переключатель точки с единственным пунктом «SiberianGym» → создана вторая точка «SiberianGym Правый берег» через «+» → автоматическое переключение в неё подтверждено (новый JWT, новый `gymId`) → раздел «Клиентская база» на новой точке показал 0 клиентов (полная изоляция данных новой точки от исходной, не «расшарены» по ошибке) → переключение обратно на «SiberianGym» через выпадающий список подтверждено (`POST /auth/switch-gym` → перезагрузка → снова исходные данные).
- Тестовая точка сети удалена из БД после проверки (напрямую через Prisma-скрипт — API для удаления точки не предусмотрен этим пунктом).

## [0.20.0] — 2026-09-17

Продолжение работы по бэклогу `prompt-dorabotki-dlya-kluba.md` — **[P0.6]**,
оформление клиента и абонемента на несовершеннолетнего. Без этого пункта
клуб не может легально принять деньги за абонемент ребёнка: 152-ФЗ и
гражданское законодательство требуют согласия законного представителя, а
не самого несовершеннолетнего.

### Добавлено
- **[P0.6] `isMinor` вычисляется динамически**, а не хранится полем в БД — `api/src/clients/age.util.ts` (`calculateAge`/`isMinor`) считает по дате рождения и настраиваемому порогу зала `Gym.selfTrainingMinAge` (по умолчанию 18, миграция `20260916204827_p0_6_guardians_minors`) при каждом чтении клиента. Осознанное решение: статус сам «переключается» в день рождения без cron-джобы и без риска рассинхронизации с реальным возрастом.
- **Сущности `Guardian`/`GuardianChild`** (та же миграция) — законный представитель может быть привязан к нескольким детям, у ребёнка может быть несколько представителей; `Guardian.linkedClientId` — опциональная связь на случай, если представитель сам является клиентом клуба.
- **`api/src/guardians/`** — новый модуль (CEO/STAFF): поиск представителя по телефону, привязка/отвязка к ребёнку (отвязка блокируется, если у ребёнка не останется ни одного представителя), создание. Заведение представителя — не self-service, оформляется администрацией по факту бумаги/присутствия на месте.
- **Блокировка на уровне бизнес-логики, не только формы:**
  - `clients.service.ts` — создание и редактирование карточки клиента отклоняется (`BadRequestException`), если дата рождения делает клиента несовершеннолетним, а тренер не указан («самостоятельные тренировки» ребёнку не положены в принципе, не только по абонементу).
  - `clients.service.ts.goSelfTraining()` — переход на самостоятельные тренировки для уже существующего несовершеннолетнего клиента отклоняется (`ForbiddenException`).
  - `orders.service.ts` (`assertGuardianConsentsIfMinor`) — оформление/продление абонемента (`MEMBERSHIP_PURCHASE`) и смена тренера/тарифа (`TARIFF_CHANGE`) для несовершеннолетнего клиента отклоняются, пока не привязан хотя бы один представитель и не зафиксированы оба его согласия (152-ФЗ + допуск к занятиям). Это тот же принцип, что и общий Order/Receipt-гейт P0.2 — деньгозначимое действие блокируется на сервере, а не только скрыто в интерфейсе.
- **[P0.6] Согласия законного представителя** — `consents.service.ts.grantForMinor`: фиксирует согласие от лица представителя (проверяется реальная привязка `GuardianChild`), тем же append-only принципом, что self-service согласия клиента (P0.4) — не редактируемая карточка, а новая запись в журнале. `statusForClient` для несовершеннолетнего клиента понижает `required` у «взрослых» типов согласия (`PDN_ADULT`/`HEALTH_DATA`/`ACTIVITY_WAIVER_ADULT`) до `false` — за ребёнка их подписывает представитель через отдельный процесс, не сам ребёнок через блокирующий экран.
- **Фронтенд:** `web/src/components/GuardianSection.tsx` — встроен в карточку клиента (`ClientsManagePage.tsx`), виден только когда `client.isMinor === true`: поиск представителя по телефону, привязка найденного или создание нового, кнопки фиксации обоих согласий по каждому представителю. Форма создания клиента блокирует кнопку «Создать клиента», если введённая дата рождения делает клиента несовершеннолетним, а тренер не выбран (`web/src/lib/age.ts` — клиентская копия серверного расчёта возраста только для подсказки в форме; окончательную проверку в любом случае делает сервер).
- **Найдено и исправлено в процессе проверки, вне первоначального скоупа пункта, но напрямую мешало его тестированию:**
  - `PersonalDataSection.tsx` показывал «взрослые» типы согласия (`PDN_ADULT`/`HEALTH_DATA`/`ACTIVITY_WAIVER_ADULT`) как обязательные и доступные для самостоятельной отметки даже несовершеннолетнему клиенту в его личном кабинете — компонент брал флаг `required` из статического текста согласия, а не из посчитанного для конкретного клиента статуса (где он уже корректно `false` для несовершеннолетнего). Юридически ребёнок не может сам дать это согласие — теперь такие строки скрываются для несовершеннолетнего клиента, остаются только добровольные маркетинговые.
  - `ClientsManagePage.tsx` — хук `useGoSelfTraining` в карточке клиента вызывался с захардкоженным `undefined` вместо id открытого клиента (существовал до P0.6, не связано с несовершеннолетними напрямую) — кнопка «Сохранить тренера и тариф» → «Самостоятельно» у ЛЮБОГО клиента всегда тихо падала («Клиент не найден», без отображения ошибки). Из-за отсутствия отображения ошибок баг был незаметен и маскировал проверку нового `ForbiddenException` для несовершеннолетних. Исправлено вместе с добавлением видимого баннера ошибки в карточке клиента (раньше падения заказа/смены тренера никак не отображались пользователю).

### Не покрыто этим пунктом (явно)
- **Только `MEMBERSHIP_PURCHASE`/`MEMBERSHIP_RENEWAL` и `TARIFF_CHANGE` проверяются на согласие представителя.** `STOCK_PURCHASE` (покупка в магазине), `LOCKER_RENTAL` (аренда шкафчика), `GROUP_CLASS_BOOKING`/`PERSONAL_SLOT_BOOKING` (запись на занятия) для несовершеннолетнего клиента серверно не блокируются — решено осознанно как минимум: это ровно то, что прямо требует юридически (оформление абонемента и допуск к тренировкам), остальное — мелкие покупки, которые не несут того же уровня юридического риска.
- **Нет уведомления в момент достижения 18 лет.** `isMinor` пересчитывается динамически при каждом чтении, поэтому статус корректно «отключается» сам, включая требования согласий и блокирующий экран для уже совершеннолетнего клиента — но ни клиент, ни администрация не получают явного уведомления о переходе; администратор узнает об этом только открыв карточку.
- **У ребёнка может быть несколько представителей — модель это поддерживает, интерфейс показывает всех**, но нет отдельного UI для назначения «основного» представителя (все считаются равнозначными для целей согласия — достаточно, что оба типа согласия зафиксированы хотя бы одним из представителей).
- **Отвязка представителя от ребёнка и повторная фиксация согласий вручную** (`POST /guardians/:id/children/:clientId/unlink`) реализованы на бэкенде, но не выведены в интерфейс — только привязка. Нужно, только если представитель поменялся (редкий случай), сделано намеренно минимально.

### Проверено
- **Backend:** `npx tsc --noEmit -p tsconfig.build.json` — чисто, без ошибок.
- **В браузере, полный сквозной сценарий под ролью STAFF:** создание клиента с датой рождения, дающей несовершеннолетний статус, без тренера — кнопка «Создать клиента» заблокирована с пояснением → выбран тренер, клиент создан → попытка «Продлить/оформить абонемент» отклонена сервером («нет законного представителя», ошибка показана в карточке) → создан представитель, привязан к ребёнку → оба согласия зафиксированы через карточку → повторная попытка оформить абонемент прошла успешно (заказ создан) → попытка перевести того же клиента на «Самостоятельно» отклонена сервером («самостоятельные тренировки недоступны младше 18 лет») → вход под выданным этому клиенту логином подтвердил, что блокирующий экран согласия (`ConsentGate`) его не блокирует (required-статус для несовершеннолетнего понижен), а раздел «Мои персональные данные» корректно не показывает «взрослые» типы согласия.
- Весь тестовый набор (клиент, связанная `User`-запись логина, представитель, тренер, временный STAFF-аккаунт, заказ, записи согласий) удалён из БД после проверки.

## [0.19.0] — 2026-09-17

Продолжение работы по бэклогу `prompt-dorabotki-dlya-kluba.md` — **[P0.5]**,
блокирующий минимум: медиа хранятся не как data URL в Postgres, а в
объектном S3-совместимом хранилище, файл принимается как multipart, а не
как base64 в JSON. Реализован именно минимум, без которого реальные фото с
телефона не работают на потоке клиентов (п. 1–3 из задачи) — Worker
Threads и асинхронная очередь (BullMQ/Redis, п. 4–5) сознательно не
делались в этой версии, они не блокируют запуск.

### Добавлено
- **[P0.5] Обязательная сверка зависимостей проведена целевым поиском по всему `web/src` и `api/src`**, а не только по `health`-модулю, как явно требует задача. Реально работающий пайплайн data-URL-загрузки оказался только один — экран клиента `ProgressPage.tsx` (фото еды, фото тела/прогресса, фото профиля) → `POST /progress-photos` и `PATCH /clients/:id`. Остальные поля, теоретически похожие по смыслу (`Exercise.imageUrl`, `TrainerCompetitionPhoto.url`, `ClubPostMedia.url`), проверены отдельно и оставлены как есть — это либо внешние CDN-ссылки (библиотека упражнений wger.de), либо поля без единого пользовательского write-пути в принципе (ни одного эндпоинта, который бы их заполнял).
- **[P0.5] Объектное хранилище** (`api/src/storage/`) — новый общий эндпоинт `POST /uploads/photo` (multipart, лимит 5 МБ на файл — страховка уже после клиентского сжатия, а не вместо него) грузит файл в S3-совместимое хранилище и возвращает URL; существующие `POST /progress-photos` и `PATCH /clients/:id` не менялись по контракту — как и раньше принимают `url: string`, просто теперь это ссылка на S3, а не data URL (добавлены `@IsUrl`/`@MaxLength(2048)` на эти поля — заодно защита от прямого вызова API в обход `/uploads/photo` с сырыми данными).
- **[P0.5] Клиентское сжатие** (`web/src/lib/imageCompress.ts`) — canvas, длинная сторона ограничена 1600px, итеративное снижение качества JPEG (до 4 попыток), пока результат не уложится в целевые ~500 КБ. `ProgressPage.tsx` (фото еды/тела/профиля) переведён с `FileReader.readAsDataURL` на `useUploadPhoto` (сжатие → `POST /uploads/photo` → готовый URL передаётся в прежние мутации без изменений их вызовов).
- **[P0.5] Согласованные лимиты на обоих уровнях, одним изменением.** `web/nginx.conf`: `client_max_body_size 8m` (с запасом над лимитом API); `api/src/main.ts`: явный `json({limit: '1mb'})` вместо дефолта body-parser (фото теперь не в JSON-теле вообще, но лимит на JSON стоит задавать явно, а не полагаться на неявный дефолт библиотеки, независимо от этого).
- **[P0.5] Понятная ошибка при превышении лимита**, не «молчаливый 413» — при первом целевом тесте (curl) превышение лимита оборвало TCP-соединение без HTTP-ответа вообще (хуже, чем просто 413, — оказалось артефактом конкретного HTTP-клиента в тестовом окружении, не сервера: повторная проверка через `fetch` подтвердила, что NestJS/Multer корректно возвращают `413 Payload Too Large` с телом). На фронте (`useUploadPhoto`) это сообщение подменяется понятным «Фото слишком большое, попробуйте другое» и показывается пользователю прямо на странице, а не проглатывается молча.
- **[P0.5] Локальная разработка — MinIO** в `docker-compose.yml` (`minio` + разовый `minio-init`, создающий бакет с публичным чтением) — не требует реальных облачных credentials для работы с этим пунктом на своём компьютере. Найдено на практике: официальный образ `minio/minio` с 2024 года публикуется на Quay.io, а не на Docker Hub — использован `quay.io/minio/minio` и `quay.io/minio/mc`.
- **[P0.5] Прод — только переменные подключения к внешнему S3**, без сервиса MinIO в `docker-compose.prod.yml` — целевая конфигурация сервера явно предполагает отдельное S3-хранилище (например Selectel S3), а не расходование места и ресурсов того же VPS. `DEPLOY_GUIDE.md` дополнен частью 6 с пошаговой организационной инструкцией (создание бакета, ключ доступа, публичный доступ на чтение) — того же типа шаг, что покупка кассы в P0.1: программная часть готова, создание самого хранилища у провайдера — решение и действие владельца клуба, не код.

### Найдено, но не в скоупе этого пункта
- **`multer` внутри `@nestjs/platform-express@11.2.3` (текущая версия в проекте) содержит известные high-severity уязвимости** (DoS через имена полей формы, file descriptor leak, race condition в `fileFilter`) — чинится только мажорным апгрейдом `@nestjs/platform-express` до v12 (и, соответственно, всего NestJS), что явно выходит за рамки одного пункта бэклога про хранение медиа. Зафиксировано как отдельный технический риск на будущее, не проигнорировано молча (`npm audit` показывает 7 high severity).

### Не покрыто этим пунктом (явно)
- **Worker Threads для серверного сжатия** (`sharp`) и **асинхронная очередь (BullMQ + Redis)** — пп. 4–5 из задачи, явно помечены в самой задаче как «не блокирует запуск, но модель данных стоит сразу спроектировать с заделом». Задел на будущее НЕ добавлен намеренно в этой версии (например, поле `status: processing | ready` у `ProgressPhoto`) — решено не усложнять модель под сценарий, которого пока нет (поток одновременных загрузок, требующий очереди), без нарушения принципа «не проектировать под гипотетические будущие требования»; при реальном упоре в этот сценарий (реальный признак — жалобы на медленную загрузку/таймауты под нагрузкой) потребуется отдельная миграция.
- **Публичность через object ACL (`public-read`)**, а не через bucket policy — работает и на MinIO, и (обычно) на S3-совместимых провайдерах вроде Selectel, но некоторые провайдеры/конфигурации могут не поддерживать ACL на уровне объекта и требовать politики на уровне бакета — если конкретный провайдер откажет, публичность нужно будет настраивать через политику бакета в его консоли (шаг 6.1.2 в `DEPLOY_GUIDE.md` уже описывает это как отдельное действие администратора).

### Проверено
- **API напрямую (curl/Node):** `POST /uploads/photo` с реальным JPEG-файлом вернул валидный S3 URL → файл подтверждённо публично доступен по HTTP без авторизации (`curl -I` → `200 OK`, `Server: MinIO`) → файл размером 6 МБ отклонён с `413 Payload Too Large` и понятным телом ответа (через `fetch`, не через `curl` — конкретно в `curl` в этом Windows/Git Bash окружении соединение обрывалось раньше, чем долетал ответ, это особенность инструмента тестирования, не сервера).
- **В браузере, сквозной сценарий:** вход клиентом → вкладка «Прогресс» → «Обо мне» → загрузка фото профиля (реальный файл, отправленный через `DataTransfer` в `<input type=file>`, так же, как это делает браузер при реальном выборе файла) → сетевой запрос `POST /uploads/photo` вернул `201 Created` → аватар на странице обновился на загруженное фото → `GET /clients/me` подтвердил, что `profilePhotoUrl` — это ссылка на MinIO (`http://localhost:9000/siberiangym-media/...`), не data URL → размер объекта в хранилище (20,9 КБ) меньше исходного файла (42,6 КБ) — подтверждает, что клиентское сжатие реально отрабатывает, а не просто прокидывает файл как есть.
- Тестовый клиент (включая связанную `User`-запись логина) и все связанные `ConsentRecord`/`ProgressPhoto`/`activity-log` записи удалены из БД после проверки.

## [0.18.0] — 2026-09-16

Продолжение работы по бэклогу `prompt-dorabotki-dlya-kluba.md` — **[P0.4]**,
152-ФЗ: согласие на обработку персональных данных. Система собирает фото
клиента, фото тела «было/стало», биометрические замеры и женский
календарь — это чувствительные персональные данные.

### Добавлено
- **[P0.4] Сущность `ConsentRecord`** (`api/prisma/schema.prisma`, миграция `20260916160918_p0_4_consent_records`) — журнал добавлений (не перезаписываемая карточка): каждое действие «дать»/«отозвать» — новая запись с версией текста, IP-адресом и User-Agent, откуда получено согласие. Текущий статус по типу — последняя запись. 8 типов согласий (`ConsentType`), из них обязательны для обычного клиента (18+) через блокирующий экран при первом входе — три: базовое согласие на ПДн, данные о здоровье/физических показателях, информированное согласие на занятия физкультурой. Остальные пять (законный представитель — ПДн/допуск несовершеннолетнего, согласие сотрудника, два маркетинговых) — не блокируют вход: маркетинговые добровольны по своей природе, guardian- и staff-варианты требуют сущностей, которых пока нет (`Guardian` — P0.6, форма заведения тренера — P1.4) — модель данных и тексты уже готовы как задел, чтобы не переделывать при появлении этих пунктов.
- **[P0.4] Тексты согласий** (`api/src/consents/consent-texts.const.ts`) — черновики из Приложения к бэклогу, с версией `2026-09-14`, зафиксированной в каждой записи `ConsentRecord`. Юридические реквизиты оператора подставляются из `GYM_LEGAL_NAME`/`GYM_CLUB_NAME` (новые переменные окружения); если не заполнены — в тексте виден явный плейсхолдер вместо выдуманных данных, как сигнал администрации клуба заполнить их реальными реквизитами перед вводом в эксплуатацию. Тексты собираются функцией при каждом обращении, а не константой на уровне модуля — иначе `process.env.GYM_LEGAL_NAME` читался бы раньше, чем `ConfigModule` успевает распарсить `.env` в локальной разработке (найдено при реализации, не гипотеза — см. комментарий в файле).
- **[P0.4] Блокирующий экран согласия при первом входе клиента** (`web/src/components/ConsentGate.tsx`, встроен в `ClientShell`) — раскрывающиеся тексты, отдельный чекбокс на каждое обязательное согласие (не один общий чекбокс «согласен со всем») плюс отдельные необязательные для двух маркетинговых типов. Кнопка «Продолжить» активна только когда отмечены все обязательные.
- **[P0.4] Раздел «Мои персональные данные»** в личном кабинете клиента (`web/src/components/PersonalDataSection.tsx`, подключён в `PaymentsPage`): статус каждого согласия, отзыв (с явным предупреждением для обязательных — «может повлечь невозможность дальнейшего оказания услуг», текст формы отзыва из Приложения к бэклогу) и выдача для добровольных, кнопка «Скачать мои данные» (выгрузка всех данных клиента одним JSON-файлом — право на выгрузку) и «Запросить удаление данных».
- **[P0.4] Разведены явно «удаляется» и «хранится обезличенно»** — ровно то, что прямо требует сам пункт задачи, чтобы не получить тихий баг, нарушающий уже другой закон. `requestDeletion` (`api/src/consents/consents.service.ts`) удаляет: фото прогресса, замеры, женский календарь, телефон, email — и отзывает маркетинговые согласия. НЕ удаляется и не переименовывается: ФИО, сама карточка клиента, абонементы, заказы, чеки, транзакции, записи в `activity-log` — они обязаны сохранять узнаваемую привязку к владельцу документа по требованиям налогового/бухгалтерского учёта (как правило не менее 5 лет), причём записи `activity-log` в принципе не могут быть отредактированы задним числом (хранят имя как текст на момент действия, а не ссылку) — сам журнал по своей природе неизменяем.
- **[P0.4] Аудит для CEO/STAFF** (`GET /consents/client/:clientId`, read-only секция в карточке клиента в `ClientsManagePage`) — подтверждение, что клиент подписал нужные формы до начала тренировок, без необходимости лезть в базу руками. Само согласие даёт только клиент через личный кабинет — администратор не может дать/отозвать согласие за клиента.

### Не покрыто этим пунктом (явно)
- **Серверный guard на каждый бизнес-эндпоинт, требующий обязательные согласия, не заводился.** Реализован только фронтенд-гейт (блокирующий экран при входе). Технически ничто не мешает клиенту с недействующим согласием обратиться к API напрямую, минуя интерфейс — решение сознательное для этой версии: цифровая фиксация факта согласия для аудита важнее полной непроходимости API, а добавление guard'а на каждый существующий и будущий бизнес-эндпоинт — отдельная по объёму задача, которую разумнее делать вместе с P0.6 (несовершеннолетние тоже требуют серверной блокировки некоторых действий).
- **Guardian-сценарий (согласие законного представителя за несовершеннолетнего) и согласие сотрудника** — модель данных и тексты готовы (см. «Добавлено»), но процесс их получения не реализован — требует `Guardian` (P0.6) и формы заведения тренера (P1.4) соответственно.
- **Политика обработки ПДн как отдельный публичный документ** (не привязанный к конкретному действию согласия, а общедоступная страница) — не заведена отдельно; тексты согласий по смыслу покрывают то же самое, но отдельного документа/страницы «Политика обработки персональных данных» в приложении нет.

### Проверено
- **API напрямую (curl):** создан тестовый клиент с логином → `/consents/texts` вернул 8 типов с корректными флагами `required` (ровно 3 обязательных для клиента) → до согласия `/consents/mine` показывал все `granted: false` → даны 3 обязательных + 1 маркетинговое → статус обновился корректно, с версией `2026-09-14` → `/consents/export` вернул полный JSON с данными клиента и историей согласий → `/consents/delete-request` обнулил телефон/email/фото профиля у клиента (имя осталось) и отозвал маркетинговые согласия → все действия отражены в `activity-log` → `GET /consents/client/:id` (от лица CEO) корректно показал итоговый статус для аудита.
- **В браузере, сквозной сценарий (клиент):** вход новым клиентом без единого согласия → показан блокирующий экран ровно с 5 релевантными пунктами (3 обязательных + 2 маркетинговых, без guardian/staff-текстов, которые клиенту не относятся — баг найден и исправлен в процессе разработки, см. историю) → отмечены 3 обязательных чекбокса → «Продолжить» открыл обычный интерфейс клиента → раздел «Мои персональные данные» на вкладке «Оплата» показал корректные статусы → клик «Отозвать» на обязательном согласии показал явное предупреждение о возможной блокировке услуг, отменено без реального отзыва.
- Тестовые клиенты (включая связанные `User`-записи логинов, которые не удаляются каскадно при удалении `Client` — отдельно почищены вручную) и все связанные `ConsentRecord`/`activity-log` записи удалены из БД после проверки.

## [0.17.0] — 2026-09-16

Продолжение работы по бэклогу `prompt-dorabotki-dlya-kluba.md` — **[P0.3]**,
HTTPS на реальном домене. Через HTTP уходят JWT, пароли при логине и
персональные/медицинские данные клиентов — это не техдолг на потом, а
условие выхода в прод.

### Добавлено
- **[P0.3] Caddy как TLS-терминатор**, встроен прямо в `docker-compose.prod.yml` отдельным сервисом перед `web` — получает и автоматически продлевает сертификат Let's Encrypt без какого-либо ручного шага после первого деплоя.
- **[P0.3] Один переключатель — переменная `DOMAIN` в `.env.prod`.** Пустая (значение по умолчанию) — Caddy слушает обычный порт 80 без TLS, приложение работает по IP, как раньше (ничего не меняется для тех, у кого домена ещё нет); заполненная реальным доменом — автоматически включается HTTPS. Решение вычисляется в `caddy-entrypoint.sh` (обычный плейсхолдер `{$DOMAIN:default}` в `Caddyfile` не подошёл — он берёт значение по умолчанию, только если переменная вообще не задана, а Docker Compose всегда передаёт `DOMAIN` в контейнер как заданную, пусть и пустую, строку; на shell-уровне `${DOMAIN:-:80}` корректно обрабатывает оба случая).
- **[P0.3] Заголовки безопасности** (HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`) добавлены на уровне Caddy для всех ответов.
- **[P0.3] Пересмотренное решение, зафиксировано явно.** Раньше (README/`DEPLOY_GUIDE.md` до этой версии) HTTPS предполагался через Caddy, установленный вручную на самом сервере (`curl -fsSL https://getcaddy.com | ...`, отдельный `Caddyfile` в системе), «в `docker-compose.prod.yml` менять ничего не придётся». Это решение пересмотрено: `web` уже публикует порт 80 контейнером — нативный Caddy на хосте физически не смог бы забиндиться на тот же порт 80 для ACME-challenge и HTTP→HTTPS редиректа. Правильная починка — TLS-терминатор внутри того же `docker-compose`, а не рядом с ним; `README.md` и `DEPLOY_GUIDE.md` (часть 5) переписаны под новый порядок действий.
- **[P0.3] Найден и исправлен риск, не связанный напрямую с HTTPS, но обнаруженный при проверке.** `docker-compose.prod.yml` не задавал явное имя проекта Docker Compose (`name:`) — при запуске из той же папки, где лежит `docker-compose.yml` (локальная БД для разработки, тоже объявляет volume `siberiangym_pg_data`), оба файла по умолчанию резолвятся в **один и тот же** volume с данными (Docker Compose берёт имя проекта из имени папки, если оно не задано явно). Воспроизведено на практике при разработке этого пункта: `docker compose -f docker-compose.prod.yml up` тихо пересоздал dev-контейнер БД, унаследовав его volume с чужими (не prod) учётными данными, из-за чего API не смог подключиться (`P1000: Authentication failed`) — реальных данных это не стёрло (volume не удалялся), но иллюстрирует, насколько тихо и незаметно могла случиться подмена данных на реальном сервере при похожей ошибке в будущем. Починка — явный `name: siberiangym-prod` в `docker-compose.prod.yml`.

### Не покрыто этим пунктом (явно)
- **Реальный сертификат Let's Encrypt не проверен** — у этой сессии нет публичного домена/DNS, доступного с машины разработки, поэтому живой ACME-challenge не тестировался. Проверено: (а) синтаксис `docker-compose.prod.yml` (`docker compose config`), (б) полный локальный прогон в режиме без домена — Caddy на `:80`, все заголовки безопасности, полный путь запроса `curl → caddy → web (nginx) → api` включая логин и получение JWT, (в) `caddy adapt` с подставленным реальным доменом отдельно от `docker compose`, подтверждающий, что конфигурация переключается в режим `:443` с автоматическим HTTPS и host-матчингом. Первую реальную проверку с настоящим доменом стоит сделать при первом деплое на сервер с уже настроенной DNS A-записью — шаги описаны в `DEPLOY_GUIDE.md`, часть 5.

### Проверено
- **Локально, оба режима работы Caddy:** конфигурация `docker-compose.prod.yml` валидна (`docker compose config`) под тестовым `.env.prod`. Полный стек (Postgres + API + web + Caddy) поднят в изолированном от dev-окружения Docker-проекте (`name: siberiangym-prod`, отдельные volumes) — без домена Caddy слушает `:80`, `curl http://localhost/` отдаёт статику с полным набором security-заголовков (включая `Strict-Transport-Security`, `Via: 1.1 Caddy`), `curl -X POST http://localhost/api/auth/login` проходит весь путь через nginx (`web`) на `api` и возвращает рабочий JWT после сидирования зала и CEO. Отдельно (`caddy adapt` вне `docker compose`) подтверждено, что при заданном `ADDR=example.com` Caddy генерирует конфигурацию с автоматическим HTTPS на `:443` вместо `:80`.
- Тестовый изолированный Docker-проект (`siberiangym-prod`, все контейнеры и volumes, включая тестовую БД) полностью удалён после проверки (`docker compose down -v`); исходное dev-окружение (`docker-compose.yml`, порт 5433) проверено на целостность данных до и после — не пострадало.

## [0.16.0] — 2026-09-16

Продолжение работы по бэклогу `prompt-dorabotki-dlya-kluba.md` — **[P0.7]**,
возврат оплаты. Идёт сразу за P0.2, так как напрямую опирается на модель
`Order`/`OrderLine`.

### Добавлено
- **[P0.7] Сущности `Refund`/`RefundLine`** (`api/prisma/schema.prisma`, миграция `20260916113803_p0_7_refund`) — по 54-ФЗ отмена платежа не «удалить транзакцию из базы», а отдельный фискальный документ «чек возврата прихода». Возврат работает той же схемой, что приём оплаты в P0.2: запрос на возврат (`POST /orders/:id/refund`, CEO/STAFF, обязательна причина) ничего не откатывает сам по себе — откат применяется только после того, как администратор пробьёт на кассе чек возврата и отсканирует его (`POST /refunds/:id/confirm-receipt`), а его сумма совпадёт с суммой возврата до копейки.
- **[P0.7] Проверка типа операции чека (n=2).** Фискальный QR несёт тег типа операции (1 — приход, 2 — возврат прихода, 3 — расход, 4 — возврат расхода) — подтверждение возврата отклоняет чек, если тип операции не «2», с понятным сообщением. Без этой проверки обычный чек прихода можно было бы случайно (или намеренно) отсканировать как возврат.
- **[P0.7] Явное решение — только полный возврат заказа целиком**, а не по отдельным позициям — осознанный выбор из двух вариантов, предложенных в задаче, чтобы не плодить неявные промежуточные состояния (частичный возврат группового чека — отдельная задача на будущее, если понадобится).
- **[P0.7] Откат по каждому типу позиции — явно продумано для каждого, не оставлено на усмотрение реализации:**
  - абонемент — восстанавливается снэпшот состояния ДО заказа (`OrderLine.meta._prevState`, сохраняется в момент применения заказа в `OrdersService.applyLine`, т.к. `Membership` — не история, а один перезаписываемый ряд); если абонемента не было вообще — запись удаляется;
  - тариф/тренер — аналогично восстанавливается снэпшот `{trainerId, tariff, format}` из `_prevState`, с новой записью в `ClientFormatHistoryEntry`, отражающей откат;
  - товар со склада — **явное решение**: продажа в P0.2 сознательно не списывает остаток (то же поведение, что было в демо-версии), поэтому и возврату физически нечего возвращать на склад — откатываются только деньги, это зафиксировано в тексте, а не молча пропущено;
  - шкафчик — освобождается, если всё ещё занят тем же клиентом (если уже переарендован администратором — не трогается);
  - групповое/персональное занятие — бронь снимается, если ещё существует и слот ещё не отмечен как проведённый/пропущенный.
- **[P0.7] Отдельная категория в отчётности, а не «тихая» отрицательная транзакция.** Новая категория `TransactionCategory.REFUND` — возврат создаёт `Transaction` с отрицательной суммой и этой категорией (а не просто уменьшает существующую запись), видна в CEO-кабинете (`web/src/features/ceo/RevenuePage.tsx`: отдельная плитка «Возвраты», сегмент в динамике по дням/месяцам; в круговой диаграмме «по источникам» не участвует — отрицательная доля там не имеет смысла, но сумма учтена в «Выручка всего»).
- **[P0.7] UI на ресепшене** (`web/src/features/shared/OrdersPage.tsx`, вкладка «Возвраты», доступна STAFF и CEO): поиск клиента → список его оплаченных заказов → запрос возврата с причиной → тот же сканирующий UI, что в P0.2 (`ReceiptScanPanel`, вынесен в переиспользуемый компонент из прежнего инлайн-кода в `OrdersPage`), теперь используется и для оплаты, и для возврата.
- **[P0.7] Один чек — один документ.** Уникальный индекс на `fn+i+fp` у `Refund` (как у `Order`) плюс явная проверка при подтверждении, что чек не использован ни как чек оплаты (в `Order`), ни как чек другого возврата — попытка фиксируется в `activity-log` как подозрительная.
- **[P0.7] Защита от повторного возврата.** Запросить новый возврат по заказу, у которого уже есть незакрытый или подтверждённый возврат, нельзя — понятная ошибка вместо второго параллельного возврата на тот же заказ.

### Не покрыто этим пунктом (явно)
- **Частичный возврат** по отдельным позициям группового чека — не реализован, задокументировано как сознательное ограничение (см. «Добавлено» выше), а не недосмотр.
- **Визуальный нюанс графика «Динамика выручки» по дням** в CEO-кабинете: если оплата и возврат по одному клиенту происходят в один календарный день, столбчатая диаграмма (`recharts`, `stackId`) может визуально наложить сегмент «Возвраты» поверх сегмента положительной категории того же дня — все цифры (плитки, легенда, «Выручка всего», сама сумма в БД) при этом корректны, страдает только пиксельный рендеринг узкого edge-case. Не является блокером — не стал глубже разбираться с рендерингом recharts со смешанными знаками ради одного тестового сценария, к реальной эксплуатации (оплата и возврат почти всегда в разные дни) это не относится.
- Привязка возврата к конкретному тренеру в «Топ тренеров по выручке» — возврат не вычитается из выручки тренера персонально (`Transaction.trainerId` у возврата не заполняется), только из общей выручки. Отдельное уточнение, если понадобится.

### Проверено
- **API напрямую (curl):** оформлен и оплачен заказ на продление абонемента (MONTHLY) → запрошен возврат с причиной → подтверждение чеком с типом операции `n=1` (обычный приход) отклонено с понятным сообщением → подтверждение тем же чеком, но с `n=2` (возврат прихода), прошло → абонемент клиента корректно вернулся к состоянию до заказа (SINGLE, без даты окончания) → в `Transaction` появилась запись категории `REFUND` на `-4500` рядом с исходной `MEMBERSHIP` на `+4500` → повторный запрос возврата по тому же (уже возвращённому) заказу отклонён.
- **В браузере, сквозной сценарий (STAFF → CEO):** создан тестовый клиент → оформлен и оплачен заказ на абонемент через раздел «Заказы» → во вкладке «Возвраты» найден клиент по имени, запрошен возврат с причиной → заказ появился в «Ожидают чека возврата» → сканирование чека с `n=1` показало ошибку прямо в интерфейсе, возврат остался открытым → сканирование корректного чека с `n=2` закрыло возврат → под учётной записью CEO раздел «Выручка» показал плитку «Возвраты: −4 500 ₽» и «Выручка всего: 0 ₽» (корректно взаимно погашено).
- Тестовый клиент, временная учётная запись администратора и все связанные заказы/возвраты/транзакции/записи журнала удалены из БД после проверки.

## [0.15.0] — 2026-09-16

Начало работы по бэклогу `prompt-dorabotki-dlya-kluba.md` (доработки для
запуска в реальном клубе). Первый пункт — **[P0.2]**, самый инвазивный в
Приоритете 0: он оборачивает все существующие денежные действия.
`P0.1` (наличие сертифицированной кассы) — организационный шаг клуба, не
код, поэтому в CHANGELOG не отражается.

### Добавлено
- **[P0.2] Сущность `Order`/`OrderLine`** (`api/prisma/schema.prisma`, миграция `20260916105812_p0_2_order_receipt_confirmation`) — промежуточное состояние между «клиент/администратор захотел что-то купить» и «деньги реально приняты». Статусы `DRAFT → AWAITING_PAYMENT → PAID/CANCELLED/EXPIRED`, `paymentMethod` (`CASH`/`CARD_ONLINE` — второй пока не используется, появится вместе с P2.9), `totalAmount` — ценовой снэпшот на момент создания заказа, не пересчитывается при оплате. Выбрана стратегия **«ничего не применяется, пока чек не подтверждён»** — рекомендованная по умолчанию в самом бэклоге как более безопасная для денег.
- **[P0.2] Приём чека сканированием** (`api/src/orders/`): `OrdersService.confirmReceipt` разбирает фискальный QR/штрихкод российской онлайн-кассы (`t=&s=&fn=&i=&fp=&n=`, `api/src/orders/receipt-qr.util.ts`), сверяет сумму чека с суммой заказа с точностью до копейки и только при совпадении применяет все позиции заказа одной транзакцией БД. При несовпадении заказ остаётся открытым, администратору показывается расхождение (сумма в чеке vs в заказе). Уникальный индекс на `fn+i+fp` (`@@unique` на `Order`) — один чек нельзя привязать к двум заказам; попытка фиксируется в `activity-log` отдельной записью «Подозрение: повторное использование чека».
- **[P0.2] Групповой чек** — `POST /orders` принимает несколько позиций разных типов в одном заказе (пример из ТЗ: продление абонемента + вода одним чеком), `totalAmount` — их сумма.
- **[P0.2] Просроченные заказы** — `AWAITING_PAYMENT`-заказ, который никто не подтвердил за `ORDER_AWAITING_PAYMENT_TIMEOUT_MINUTES` (по умолчанию 120 мин.), автоматически переходит в `EXPIRED` фоновым интервалом внутри процесса `OrdersService` (без внешнего планировщика — в проекте пока нет `@nestjs/schedule`/BullMQ; интервал — сознательно простое решение для одного инстанса API, до перехода на кластер в P3.18).
- **[P0.2] Заказ не привязан намертво к администратору-автору** — видимость и право закрыть заказ (`GET /orders/open`) определяются по `gymId`, а не по `createdBy`: другой администратор на смене видит и может закрыть тот же заказ.
- **[P0.2] Рефакторинг денежных путей.** Обязательная сверка зависимостей перед P0.2 нашла **7** мест, напрямую создававших `Transaction` (в задаче явно перечислено 6 — седьмое, `ClientsService.chooseTrainer`, найдено дополнительно при код-ревью и обёрнуто туда же, в `TARIFF_CHANGE`): `purchase-membership`, `change-tariff`, `choose-trainer` (`api/src/clients/`), `stock/purchase` (`api/src/stock/`), аренда шкафчика (`api/src/lockers/`), запись на групповое/персональное занятие (`api/src/schedule/`). Старые эндпоинты, создававшие `Transaction` напрямую и минуя подтверждение чеком, удалены — не оставлено два параллельных пути. Категория `Transaction.category` (уже существующий enum, используемый в CEO-отчётности по выручке — `web/src/features/ceo/RevenuePage.tsx`) выводится из `OrderLine.type` маппингом внутри `OrdersService`, а не задублирована отдельной логикой группировки — фронтенд отчёта по выручке не менялся.
- **[P0.2] Сканер на ресепшене** (`web/src/features/shared/OrdersPage.tsx`, доступна и STAFF, и CEO): текстовое поле с автофокусом принимает ввод обычного USB/BT-сканера штрихкодов (работает как клавиатура, Enter отправляет на подтверждение) — без какого-либо специального драйвера; плюс сканирование камерой устройства через `@zxing/browser` (`web/src/components/QrCameraScanner.tsx`) для администраторов без выделенного сканера.
- **[P0.2] Клиентский сценарий оплаты наличными.** Кнопки покупки (абонемент/продление, тариф с тренером, вода/еда, шкафчик, запись на групповое/персональное занятие — `PaymentsPage`, `ShopPage`, `TrainerSelectionPage`, `CalendarPage`) вместо мгновенного применения теперь собирают заказ и сразу отправляют его в `AWAITING_PAYMENT` (`useCreateCashOrder`, `web/src/hooks/useOrdersApi.ts`), показывая клиенту/администратору понятное «Заказ оформлен на N ₽ — оплатите наличными на ресепшене» (`web/src/components/OrderPendingNotice.tsx`). Кнопка «Оплатить картой онлайн» не показывается нигде — P2.9 не реализован, это осознанное ограничение MVP, а не недоделанность.

### Не покрыто этим пунктом (явно, чтобы не считать P0.2 закрытым полностью)
- **Начальная покупка абонемента при регистрации клиента** (`ClientsService.create()`, форма «Новый клиент» у администратора) по-прежнему не создаёт `Transaction` вообще — это не входило в перечень денежных путей из задачи (в ней шла речь о `purchase-membership`/продлении, а не о первичной регистрации) и не переведено на `Order`. Отдельная задача на будущее, если это тоже должно идти через подтверждение чеком.
- **Гонки при одновременном захвате ограниченного ресурса** (шкафчик/слот/место в группе) в окне между сборкой заказа и подтверждением чека — `applyLine` ревалидирует ресурс прямо перед применением и вернёт понятную ошибку («кто-то занял, пока заказ ожидал оплаты»), но без блокировки на уровне БД-транзакции с изоляцией — это отдельный пункт бэклога (P3.10), сознательно не в этом PR.
- **Сканирование камерой** реализовано и собирается, но не проверено на реальном устройстве с камерой в этой сессии (песочница без физической камеры) — проверен только ввод сканером-клавиатурой/вручную, см. «Проверено».
- **P0.1** (сертифицированная касса как таковая) — организационный шаг клуба (покупка и регистрация в ФНС), не код; в CHANGELOG не отражается отдельным пунктом.

### Исправлено
- **`api/dist` собирался с устаревшим кэшем инкрементальной компиляции** (`tsconfig.build.tsbuildinfo`) — после переноса кода между модулями часть скомпилированных файлов (например, `dist/prisma/prisma.module.js`) не пересоздавалась, и собранный сервер падал с `MODULE_NOT_FOUND` при старте. Починка — полная пересборка (`rm -rf dist tsconfig.build.tsbuildinfo && nest build`); не связано напрямую с P0.2, но обнаружено и починено при проверке этого пункта.

### Проверено
- **API напрямую (curl):** вход CEO → создан тестовый клиент и товар → заказ из двух позиций (`STOCK_PURCHASE` + `MEMBERSHIP_PURCHASE`) → `totalAmount` посчитан верно → отправка на оплату наличными → подтверждение чеком с неверной суммой отклонено с понятным сообщением, заказ остался открытым → подтверждение тем же QR с верной суммой закрыло заказ, применило продление абонемента (`expiresAt` пересчитан) и создало 2 `Transaction` с правильными категориями и `orderId` → повторное использование того же чека (`fn+i+fp`) для другого заказа отклонено с 409 и зафиксировано в `activity-log` как подозрительное.
- **В браузере, реальный сквозной сценарий (STAFF):** вход администратором → создан тестовый клиент → «Продлить/оформить абонемент» создал заказ и показал `OrderPendingNotice` → в разделе «Заказы» заказ виден в очереди с обратным отсчётом до истечения → подтверждение чеком с неверной суммой показало ошибку расхождения прямо в интерфейсе, заказ остался в очереди → подтверждение корректным чеком закрыло заказ (очередь опустела), в журнале изменений появилась запись «Подтвердил оплату заказа чеком» → повторный заказ отменён вручную из раздела «Заказы» («Отменить заказ» → причина → «Подтвердить отмену»), очередь снова опустела.
- Тестовый клиент, временная учётная запись администратора, тестовый товар и все связанные заказы/транзакции/записи журнала удалены из БД после проверки.

## [0.14.0] — 2026-09-11

Расширена библиотека упражнений и подключены реальные фото техники
выполнения из открытой базы данных.

### Добавлено
- **Библиотека упражнений выросла с 50 до 93** — добавлены 43 новых упражнения (жимы на наклонной/отрицательной скамье, дополнительные варианты тяг и приседаний, изоляция на руки и пресс, кардио-варианты) с оригинально написанными русскими названиями и техникой выполнения.
- **`imageUrl`/`imageAttribution`** у модели `Exercise` (миграция `20260911234143_exercise_images`) — 71 упражнение (из 93) теперь имеет реальное фото техники выполнения вместо иллюстративного плейсхолдера; остальные по-прежнему используют плейсхолдер на фронтенде.
- **`web/public/exercises/`**: 71 изображение (~3.6 МБ суммарно, CDN-миниатюры 400×400) из открытой базы упражнений [wger.de](https://wger.de) — Django REST API с явной лицензией на каждое изображение (Creative Commons CC-BY-SA 3.0/4.0, автор фиксируется в `license_author`).
- Библиотечная карточка на вкладке «Библиотека» показывает настоящее фото и подпись автора (`Фото: wger.de, CC-BY-SA X.0, автор: …`), под списком — общая ссылка на лицензию.

### Отклонено (важно для дальнейшей работы с ассетами)
- **APK конкурента (GymUp)** как источник фото — извлечение проприетарных фотографий из чужого коммерческого приложения для вставки в наш продукт является нарушением авторских прав и ToS; не делалось.
- **free-exercise-db** (изначально предложен как альтернатива) — отклонён после проверки: код/JSON под Unlicense (public domain), но лицензия самих изображений в репозитории не документирована, и в issues репозитория есть два открытых нерешённых вопроса именно об этом («License of Images?», «images has copyright?»). Использовать в коммерческом продукте небезопасно. Вместо этого выбран wger.de, где лицензия указана явно и принудительно на каждой картинке через API.
- Текстовые описания wger (на английском) не переводились и не использовались — только фото. Названия упражнений и техника выполнения на русском написаны заново, чтобы не создавать производный текст от чужого описания и не зависеть от специфики условий Share-Alike для текста.

### Исправлено
- **`prisma/seed.ts`**: сидирование библиотеки упражнений было one-shot ("если у зала уже есть хоть одно упражнение — пропустить всё") — на уже развёрнутой базе новые упражнения из библиотеки никогда бы не попали в БД. Теперь сидирование добавляет только отсутствующие по имени упражнения и отдельно дозаполняет фото (`imageUrl`/`imageAttribution`) у упражнений, которые были засеяны раньше без них — без повторного создания дублей и без затрагивания остальных данных.

### Проверено
- Локально: пересборка API, `prisma generate`, повторный запуск сида на уже заполненной dev-базе — добавились ровно 43 новых упражнения, затем 28 старых по имени дозаполнились фото; третий запуск сида не внёс изменений (идемпотентность).
- В браузере (клиентский кабинет, вкладка «Библиотека»): реальные фото отображаются с подписью автора, упражнения без фото показывают прежний иллюстративный плейсхолдер, все 71 изображение отдаются со статусом 200. Тестовый клиентский аккаунт, созданный для проверки, удалён после теста.

## [0.13.0] — 2026-09-11

Docker-развёртывание — приложение собирается и разворачивается одной
командой `docker compose -f docker-compose.prod.yml up -d --build`.
Проверено полностью локально: Postgres + API + фронтенд (nginx) подняты
вместе, миграции и сид отработали, вход по HTTP через nginx на порту 80
вернул настоящий JWT.

### Добавлено
- **`api/Dockerfile`**: многоступенчатая сборка (сборка Nest → рантайм-образ без dev-зависимостей), миграции (`prisma migrate deploy`) применяются автоматически при каждом старте контейнера.
- **`web/Dockerfile` + `web/nginx.conf`**: сборка Vite-статики, отдаётся nginx'ом; тот же nginx проксирует `/api/*` на контейнер backend — фронтенд и API на одном порту, без CORS и без отдельного публичного адреса для backend.
- **`docker-compose.prod.yml`** + **`.env.prod.example`**: Postgres + API + web одним файлом, все секреты — через `.env.prod` (не коммитится).
- **README**: инструкция по развёртыванию, обновлению, будущему HTTPS через Caddy (когда появится домен) и рекомендация по хостингу — недорогой надёжный VPS в российской юрисдикции (Timeweb Cloud в первую очередь, Selectel — если важнее SLA, чем цена).

### Исправлено
- **`prisma/seed.ts` не запускался в контейнере**: `ts-node` в рантайм-образе зависел от версии Node внутри контейнера — на новых 22.x (не совпадающих с версией, под которую разрабатывалось на этой машине) загрузчик `.ts`-файлов ведёт себя иначе и ломает ts-node. Сид теперь компилируется в обычный JS прямо на этапе сборки образа (`api/Dockerfile`) и запускается как `node dist/prisma-seed/seed.js` — не зависит от ts-node и версии Node в рантайме.
- **Пересечение имён контейнеров с dev-окружением**: `docker-compose.prod.yml` использовал те же `container_name`, что и локальный `docker-compose.yml` для разработки — на одной машине они конфликтовали. Убраны, имена теперь генерируются Docker Compose по имени проекта.
- **Prisma в Docker не находила версию OpenSSL** (предупреждение при генерации клиента) — добавлена явная установка `openssl` в оба этапа сборки `api/Dockerfile`.

### Проверено
- Полный цикл локально в Docker: `docker compose -f docker-compose.prod.yml build` → `up -d` → миграции применились автоматически → `node dist/prisma-seed/seed.js` создал зал/CEO/библиотеку упражнений → `curl` на `http://localhost/api/auth/login` через nginx вернул рабочий JWT → статика (`index.html`, `manifest.webmanifest`) отдаётся корректно. Тестовый стек и образы удалены после проверки.

## [0.12.0] — 2026-09-11

Email-уведомления — единственный канал на старте (без SMS, как решено
в 0.1.0).

### Добавлено
- **`api/src/email/`**: `EmailService` на `nodemailer` с SMTP через `.env` (`SMTP_HOST/PORT/USER/PASS/FROM`). Пока SMTP не настроен, письма только логируются в консоль — деплой без готовой почты не ломается, а после добавления реквизитов рассылка включается сама, без изменения кода.
- Письма уходят на три реальных события: выдача доступа в приложение (клиенту/тренеру — email и временный пароль), оформление/продление абонемента (подтверждение), ответ директора на обращение клиента.

### Проверено
- Без настроенного SMTP: выдача доступа и оформление абонемента корректно залогировали оба письма (тема + текст) вместо отправки, ошибок нет.

## [0.11.0] — 2026-09-11

PWA-обвязка — приложение можно установить на экран телефона как
"настоящее" приложение, без публикации в App Store/Google Play (решение
зафиксировано ещё в 0.1.0 из-за сроков).

### Добавлено
- **`vite-plugin-pwa`**: манифест (`SiberianGym`, тема в фирменном синем `#0369a1`, `display: standalone`), service worker с офлайн-кэшем статики (запросы к `/api/*` — только сеть, реальные данные никогда не кэшируются как статика).
- **Иконки приложения**: сгенерированы напрямую (192/512/512-maskable/apple-touch-icon) — фирменный градиент с иконкой штанги, без внешних ассетов.
- `index.html`: `apple-touch-icon`, `theme-color`, `lang="ru"`, описание — для корректного вида при добавлении на экран телефона на iOS и Android.
- Убраны неиспользуемые файлы из шаблона Vite (`favicon.svg`, `icons.svg` от демо-страницы заглушки, которых в проекте больше нет).

### Проверено
- Продакшн-сборка (`vite build` + `vite preview`): манифест и иконки отдаются корректно, `sw.js` генерируется и валиден. Саму регистрацию service worker в этой сессии проверить не удалось — песочница превью-браузера блокирует `navigator.serviceWorker.register()` (сам файл при этом отдаётся нормально) — это ограничение окружения предпросмотра, а не приложения; конфигурация стандартная и должна нормально работать в реальном браузере на телефоне.

## [0.10.0] — 2026-09-11

Кабинет CEO — последняя из четырёх ролей. Все разделы демо перенесены
на реальные данные: загрузка тренеров, посещаемость, выручка,
клиентская база, склад, обслуживание клуба, рассылки, журнал
изменений. **С этой версии весь функционал демо перенесён на реальный
backend для всех четырёх ролей** (Клиент, Тренер, Администратор, CEO).

### Добавлено
- **`GET /transactions`** (CEO): все финансовые транзакции зала — раньше данные о продажах нигде не были доступны за пределами самого создания записи, выручку посчитать было не из чего.
- **`GET /workout-logs`** (CEO/STAFF): все тренировки по залу (не по одному клиенту) — нужно для графика посещаемости.
- **`web/src/features/ceo/`**: загрузка тренеров (график + карточка добавления тренера с выдачей доступа), посещаемость (динамика по дням/неделям, время посещений, занятость тренеров по часам), выручка (по дням/месяцам/источникам, топ тренеров), клиентская база (формат/статус абонементов, смены тренера), карточка тренера (подопечные, платежи), обслуживание (оборудование + клининг + обращения к директору в одном месте), рассылки (CRUD предложений по аудитории), журнал изменений (полная история действий всех ролей).
- Добавление тренера доступно прямо из «Загрузки тренеров» — раньше тренеров можно было завести только напрямую через API.

### Исправлено
- **`GET /clients`** не отдавал историю смены тренера/формата (`formatHistory`) — из-за этого раздел «Клиентская база» не мог показать, кто менял тренера.

### Сознательно не перенесено (зафиксировано на будущее)
- **Экспорт отчётов в PDF** (`utils/clientReport.ts` в демо, ~570 строк генерации PDF по клиенту/тренеру/всем активным) — большой самостоятельный кусок функциональности, не критичный для запуска. Экран и данные, из которых отчёт строился бы, уже на месте (карточка тренера, клиентская база) — добавить экспорт можно отдельной задачей.

### Проверено
- Полный сквозной сценарий CEO: создан тренер с выдачей доступа → клиент выбрал этого тренера с тарифом «Ведение» и продлил абонемент → выручка (9 400 ₽) корректно разбита по категориям → карточка тренера показывает того же клиента и тот же платёж → клиентская база и журнал изменений (29 записей) отражают все действия → рассылка создана и сохранена.
- Очистка: после каждой проверки тестовые данные (клиенты, тренеры, транзакции, журнал) удалены из базы.

## [0.9.0] — 2026-09-11

Кабинет администратора (Staff) — последняя из четырёх ролей с полным
интерфейсом. Регистрация клиентов и продление абонементов, публикация
новостей, ежедневная уборка, ППР оборудования и склад — всё на
реальном API, ничего на моках.

### Добавлено
- **`web/src/features/staff/`**: список/поиск/сортировка клиентов с полной карточкой редактирования (данные, тренер и тариф, продление абонемента, выдача доступа в приложение), публикация новостей клуба, ежедневный чек-лист уборки (7 зон, создаётся на сегодня вручную — раньше в демо чек-листы были только в сид-данных), ППР с формой добавления оборудования, склад (справочник товаров, приход, списание, инвентаризация, история).
- **`web/src/features/shared/StockPage.tsx`**, **`components/EquipmentServiceList.tsx`**, **`components/ClientOutreachList.tsx`**: общие компоненты, переиспользуются между Staff и (в следующей версии) CEO — как и в демо.

### Проверено
- Вход администратором → создан клиент → выдан доступ в приложение (email+пароль) → создан чек-лист уборки на сегодня, отмечен один пункт → товар добавлен в справочник → приход +10 на склад, отражён в остатках и истории.

## [0.8.0] — 2026-09-11

Кабинет тренера — список подопечных, карточка клиента (программа,
история тренировок, прогресс по мышцам, замеры, фото, обратная связь)
и личное расписание. Плюс важная инфраструктурная вещь: раньше у CEO
физически не было способа выдать клиенту или тренеру доступ в
приложение — только я мог это сделать вручную через базу при
тестировании. Теперь это нормальный эндпоинт.

### Добавлено
- **`POST /clients/:id/create-login`, `POST /trainers/:id/create-login`** (CEO/STAFF): выдаёт клиенту/тренеру доступ в приложение — создаёт учётную запись и привязывает её к уже существующей карточке. До этого карточку можно было завести, а войти в приложение под ней было физически нечем.
- **`GET /trainers/me`, `GET /trainers/me/clients`**: тренер видит свой профиль и список своих подопечных.
- **`GET /clients/:id`** теперь доступен тренеру — но только на своих подопечных (проверка владения), не на любого клиента зала.
- **`web/src/features/trainer/`**: список подопечных с фильтром по формату, карточка клиента (редактирование программы, график прогресса рабочего веса по ключевому упражнению, история тренировок, диаграмма нагрузки по мышцам, замеры, фото от клиента, переписка), расписание тренера (список по неделям + календарь).

### Проверено
- Вход тренером → список из одного назначенного подопечного → открыл карточку → добавил день в программу (сохранилось, `assignedBy: trainer`) → расписание отображается корректно (пусто, слотов ещё нет).

## [0.7.0] — 2026-09-11

Разделы «Новости» и «Прогресс» клиента — раздел «Клиент» полностью
готов, все 7 вкладок работают на реальном API. Перенесены реальные
иллюстрации анатомии мышц из демо (не сгенерированные, а настоящие
PNG) для диаграммы нагрузки.

### Добавлено
- **`GET /workout-logs/leaderboard?period=day|week|month`**: рейтинг клиентов по суммарно поднятым килограммам — кросс-клиентская агрегация, раньше такого эндпоинта не было (клиент физически не мог увидеть рейтинг, только свои данные).
- **`web/src/features/client/NewsPage.tsx`**: лента новостей клуба + рейтинг по нагрузке за день/неделю/месяц.
- **`web/src/features/client/ProgressPage.tsx`**: диаграмма нагрузки по мышцам (реальные анатомические иллюстрации, перенесены из демо в `web/public/muscles/`), замеры тела (включая биоимпедансометрию) — доступны с тарифа «Ведение», фото «было/стало» и фото еды по приёмам пищи (загрузка через файл, хранится как data URL), женский календарь (расчёт фазы цикла и прогноз следующего), редактирование ФИО/даты рождения/фото профиля.
- **`PATCH /clients/:id`** теперь также принимает `birthday` и `profilePhotoUrl` — раньше DTO их не проверял и сервис не сохранял, хотя клиенту эти поля нужны для профиля.

### Проверено
- Клиент с тарифом «Ведение»: сохранил замер (вес), клиентка отметила начало цикла (расчёт «день 1 из 28», фаза «Менструация», прогноз следующего цикла — верно) и отредактировала профиль.
- Рейтинг по нагрузке и лента новостей корректно тянутся с реального backend.

## [0.6.0] — 2026-09-11

Раздел «Упражнения» клиента — полностью на реальном API: программа
тренировок, библиотека упражнений (перенесена из демо, 50 позиций) и
логирование тренировки с таймером отдыха. Проверено в браузере: клиент
без тренера добавил день, добавил упражнение, провёл тренировку —
статус «Выполнено» посчитался автоматически.

### Добавлено
- **Библиотека упражнений заполнена реальными данными**: те же 50 упражнений, что и в демо (перенесены как есть — названия, техника, инвентарь), теперь сидируются в базу при первом развёртывании (`api/prisma/exercise-library.ts` + `seed.ts`), а не хардкодятся во фронтенде. CEO/STAFF/тренер могут добавлять свои через `POST /exercises`.
- **Группа мышц «Кардио»**: в демо она была, в изначальной схеме забыл — добавлена миграция `MuscleGroup` enum.
- **`web/src/features/client/ExercisesPage.tsx`**: три вкладки — программа (самостоятельный клиент редактирует дни/упражнения, у клиента с тренером — только просмотр), библиотека (фильтр по группе мышц, иллюстративные плейсхолдеры техники — перенесены из демо без изменений), история тренировок.
- **`WorkoutLogger`**: пошаговое логирование тренировки по факту (вес/повторы/уровень нагрузки на подход) с таймером отдыха между подходами (со звуковым сигналом через Web Audio API) — перенесено из демо.

### Проверено
- Клиент без тренера: добавил день тренировки → добавил упражнение из библиотеки → провёл тренировку через `WorkoutLogger` → статус «Выполнено» рассчитан на backend автоматически по факту выполненных подходов → отражён в истории.

## [0.5.0] — 2026-09-11

Старт фронтенда (`web/`, Vite + React 19 + TypeScript). Реальный вход по
JWT, ролевой роутинг на все четыре роли, и полностью рабочий раздел
клиента (оплата, тренер, календарь, магазин) — проверено в браузере на
реальных данных через настоящий backend, а не моки.

### Добавлено
- **Каркас фронтенда** (`web/`): Vite + React 19 + TypeScript, Tailwind CSS v4 (визуальный стиль перенесён из демо без изменений, как и просил заказчик), React Router v7 (`HashRouter` — как в демо, чтобы не требовать серверных правил переадресации на дешёвом хостинге), TanStack Query v5 для серверного состояния (вместо localStorage-как-базы из демо), Zustand — только для локального состояния авторизации.
- **Экран входа** (`web/src/pages/LoginPage.tsx`): вход по email/паролю, сохранение JWT и роли, редирект в кабинет нужной роли.
- **Ролевой роутинг**: `/client`, `/trainer`, `/staff`, `/ceo` — каждый защищён своей ролью (`RequireRole`), попытка зайти не в свой раздел редиректит в свой кабинет. Разделы Тренера/Администратора/CEO пока заглушки — появятся в следующих версиях; сейчас полностью готов раздел Клиента.
- **Раздел клиента** — перенесён из демо на реальный API через React Query:
  - **Оплата**: текущий абонемент, покупка/продление, тариф сопровождения, обратная связь директору.
  - **Тренер**: список тренеров, выбор/смена тренера и тарифа, переход на самостоятельные тренировки.
  - **Календарь**: групповые занятия (запись/отмена) и персональные слоты (для тарифа «Индивидуальные тренировки»), список и календарная сетка.
  - **Магазин**: аренда/освобождение шкафчика, покупка воды и еды из справочника.

### Добавлено (backend, по ходу подключения фронтенда)
- `GET /clients/me` — собственная карточка клиента (резолвится по токену, не по параметру).
- `GET /workout-logs/mine`, `GET /measurements/mine`, `GET /progress-photos/mine` — раньше эти данные мог читать только тренер/CEO/STAFF, сам клиент свои же тренировки/замеры/фото прочитать не мог.
- `GET /pricing` — цены абонементов и услуг зала (раньше нигде не отдавались наружу, хотя использовались в покупке).
- `POST /stock/purchase` + `GET /stock/catalog` открыт клиенту — покупка воды/еды в приложении (как в демо: фиксирует транзакцию, без учёта остатков).
- `trainers.findAll` теперь возвращает часы работы/сертификаты/фото тренера (нужно для карточки тренера в приложении клиента), раньше это отдавалось только в чтении одного тренера.

### Исправлено
- **Дыра в авторизации**: `GET /clients/:clientId/program` и `GET /clients/:clientId/feedback` не проверяли, что клиент запрашивает СВОЮ карточку — авторизованный клиент мог подставить чужой id и прочитать чужую программу тренировок или чужую переписку с тренером. Добавлена проверка владения, как уже было сделано в остальных self-service методах.
- **`RootLayout` не пускал на экран входа**: компонент возвращал `null` целиком, если пользователь не авторизован, из-за чего дочерний редирект на `/login` никогда не успевал сработать — приложение показывало пустую страницу вместо формы входа. Исправлено: неавторизованный пользователь всегда получает `<Outlet />`.

### Проверено
- Полный сквозной сценарий в браузере на реальном backend (без моков): вход клиентом → покупка абонемента → выбор тренера и тарифа → запись на групповое занятие → аренда шкафчика → покупка воды — каждое действие подтверждено в интерфейсе и очищено из базы после проверки.
- Вход CEO → ролевой редирект в `/ceo`, попытка клиента зайти в чужой раздел корректно отбрасывает в свой.

### Дальше по плану
- Разделы «Упражнения», «Прогресс», «Новости» клиента (сейчас заглушки).
- Кабинеты Тренера, Администратора и CEO.
- PWA-обвязка, деплой.

## [0.4.0] — 2026-09-11

Backend теперь покрывает весь функционал демо-версии на уровне API —
все 21 бизнес-модуль перенесены и проверены.

### Добавлено
- **Упражнения** (`api/src/exercises/`): справочник, доступен всем ролям, создают тренер/CEO/STAFF.
- **Программы тренировок** (`api/src/programs/`): у клиента ровно одна актуальная программа (`Program.clientId` теперь уникальный), задаётся тренером/CEO/STAFF или самим клиентом — при обновлении дни и упражнения полностью пересоздаются.
- **Логи тренировок** (`api/src/workout-logs/`): клиент логирует выполнение самостоятельно, статус (выполнено/частично/пропущено) считается автоматически по факту выполненных подходов.
- **Шкафчики** (`api/src/lockers/`): аренда (с транзакцией по цене за день) и освобождение.
- **Новости клуба** (`api/src/club-posts/`): публикация администрацией, видна всем ролям.
- **Обратная связь клиент↔тренер** (`api/src/feedback/`): переписка по карточке клиента.
- **Здоровье клиента** (`api/src/health/`): фото прогресса, замеры (включая биоимпедансометрию), женский календарь — всё строго self-service, привязано к собственной карточке клиента через токен.

### Проверено
- Полный self-service сценарий от лица реального клиентского логина (не администратора): чтение своей программы, логирование тренировки, добавление замера, отметка в женском календаре, сообщение тренеру, аренда шкафчика — всё корректно резолвится по `userId` из токена, а не по параметру из запроса (нельзя подменить чужого клиента).
- Журнал изменений корректно НЕ фиксирует рутинные самостоятельные действия клиента (бронирования, логи тренировок) — так же, как было осознанно решено в демо-версии, чтобы не размывать журнал шумом.

## [0.3.0] — 2026-09-11

Перенесены все оставшиеся разделы демо-версии на реальный backend: расписание,
склад, оборудование/ППР, клининг, обращения к директору, рассылки, обзвон
клиентов. Модель данных из 0.1.0 полностью ожила.

### Добавлено
- **Расписание** (`api/src/schedule/`): групповые занятия (создание, запись клиента, отмена) и персональные слоты (создание тренером, запись, отмена, отметка явки/неявки). Запись создаёт транзакцию по цене зала/тренера, как в демо.
- **Склад** (`api/src/stock/`): справочник товаров, приход, списание (с обязательной причиной — истёк срок/повреждено/продано без чека/использовано клубом/утеряно/другое), инвентаризация с автоматической корректировкой остатков (излишек — новой партией, недостача — списанием со старейших по сроку годности партий), история операций. И CEO, и STAFF управляют складом наравне.
- **Оборудование и ППР** (`api/src/equipment/`): создание карточки оборудования, отметка планового обслуживания выполненным с автоматическим расчётом следующего срока по интервалу.
- **Клининг** (`api/src/cleaning/`): чек-лист на 7 зон создаётся автоматически при создании дня уборки, отметка/снятие отметки по каждому пункту.
- **Обращения к директору** (`api/src/director-messages/`): клиент пишет, CEO отвечает, отметка "прочитано" по ответу.
- **Предложения для рассылки** (`api/src/report-offers/`): создание, включение/выключение, удаление — доступно только CEO.
- **Обзвон клиентов** (`api/src/outreach/`): список клиентов с последней заметкой по звонку; записывать результат звонка может только STAFF (Администратор), CEO видит в режиме просмотра — как и в демо-версии.

### Исправлено
- **Утечка хеша пароля**: `POST /auth/users` возвращал весь объект пользователя, включая `passwordHash` (в захешированном виде, но всё равно не должен покидать сервер). Теперь ответ явно ограничен нужными полями.

### Проверено
- Полный сквозной сценарий по всем новым разделам одним скриптом: тренер → клиент → товар → приход (+50) → списание (−5, причина «повреждено») → остаток 45 подтверждён → инвентаризация (пересчитали на 40) → автоматическая недостача −5 подтверждена → оборудование с ТО и пересчётом следующей даты → чек-лист уборки на 7 пунктов → групповое занятие с записью клиента → персональный слот с записью и отметкой явки → предложение для рассылки → обзвон клиента (под правильной ролью STAFF, доступ CEO корректно отклонён 403).

## [0.2.0] — 2026-09-11

### Добавлено
- **Журнал изменений** (`api/src/activity-log/`) — единая точка записи "кто/что/когда/на что повлияло" для всех значимых действий в системе, доступна CEO через `GET /api/activity-log`. Переиспользуется всеми остальными модулями.
- **Модуль тренеров** (`api/src/trainers/`): список (видят все роли), просмотр карточки, создание тренера (CEO/STAFF).
- **Ключевые действия с клиентом** — перенесены из демо-версии на реальный API:
  - `POST /clients/:id/choose-trainer` — выбор тренера и тарифа, с историей смены формата и транзакцией.
  - `POST /clients/:id/change-tariff` — смена тарифа.
  - `POST /clients/:id/go-self-training` — переход на самостоятельные тренировки.
  - `POST /clients/:id/purchase-membership` — оформление/продление абонемента по ценам зала.
  - Эти действия доступны и CEO/STAFF (управление любым клиентом), и самому клиенту (только своей карточкой) — проверка встроена в сервис.

### Проверено
- Полный сценарий на реальных запросах: создание тренера → создание клиента → выбор тренера с тарифом «Ведение» (формат автоматически стал «Группа», история форматов корректно закрылась и открылась заново) → продление абонемента → журнал изменений содержит все 4 действия в правильном порядке с точными деталями.

## [0.1.0] — 2026-09-11

Старт рабочей (не демо) версии SiberianGym. Заложен фундамент: модель данных,
авторизация, первый защищённый модуль — проверено реальными запросами
end-to-end (вход → токен → создание клиента → чтение из базы).

### Решения по проекту (зафиксировано на будущее)
- **Оплата абонементов пока вручную** (администратор отмечает "оплачено"), без реальной интеграции с банком — сделано намеренно, чтобы не ждать оформления ИП/юрлица. Архитектура платежей построена так, чтобы позже подключить настоящий эквайринг (скорее всего Альфа-Банк), не переделывая остальное: как только будет ИП — добавляем реальную оплату картой на этом же месте.
- **Мультиарендность заложена с первого дня**: у каждой таблицы в базе есть привязка к залу (`gymId`). Сейчас в системе один зал — SiberianGym, но добавить второй зал для продажи системы другим клубам — это создать новую запись, а не переделывать структуру.
- **"Мобильная версия" реализуется как PWA** (сайт, устанавливаемый на экран телефона как приложение), а не отдельные приложения под iOS/Android — из-за сроков (4 недели) и того, что публикация в App Store/Google Play требует отдельного длительного процесса модерации и платных аккаунтов разработчика. Отдельная задача на будущее, если понадобятся "настоящие" приложения из сторов.
- **Уведомления на старте — только email** (без SMS, дороже и не обязательно для первого запуска).

### Добавлено
- **Backend** (`api/`, NestJS + TypeScript): каркас приложения, конфигурация через `.env`.
- **База данных** (`api/prisma/schema.prisma`, PostgreSQL + Prisma): полная модель данных перенесена из демо-версии — клиенты, тренеры, абонементы, тренировки, программы, групповые занятия, персональные слоты, шкафчики, склад (партии/списания/приходы/инвентаризация), оборудование и ППР, клининг, обращения к директору, предложения для рассылки, журнал изменений, обзвон клиентов. Плюс новое: `Gym` (зал/арендатор), `User` (учётная запись для входа), `MembershipPricing` (цены теперь настраиваются на уровне зала, а не зашиты в коде).
- **Авторизация** (`api/src/auth/`): вход по email/паролю (JWT), хеширование паролей (bcrypt), защита эндпоинтов по ролям (CEO/STAFF/TRAINER/CLIENT) через guard'ы, привязка каждого запроса к своему залу.
- **Модуль клиентов** (`api/src/clients/`): создание клиента с автоматическим расчётом даты окончания абонемента (перенесено из логики демо-версии), список, просмотр, редактирование — с проверкой ролей и изоляцией по залу.
- **Seed-скрипт** (`api/prisma/seed.ts`): создаёт зал SiberianGym и первую учётную запись CEO при первом развёртывании.
- **Docker Compose** для локальной PostgreSQL при разработке.

### Проверено
- Полная цепочка на реальных HTTP-запросах: вход → JWT-токен → создание клиента (с кириллицей — проверено, что база хранит корректно) → чтение списка клиентов. Доступ без токена и с неверным паролем корректно отклоняется (401).
- `npx nest build` — сборка backend без ошибок.
- Миграция применяется к чистой базе без ошибок, создаёт 42 таблицы.

### Дальше по плану
- Перенос остальных бизнес-модулей backend (тренеры, тренировки, расписание, склад, обслуживание, отчёты и т.д.) по образцу модуля клиентов.
- Перенос экрана логина и интерфейса из демо-версии (`E:\project_gym`) на реальные запросы к API вместо localStorage.
- PWA-обвязка (иконка на экране телефона, офлайн-заглушка).
- Подключение email-уведомлений.
- Развёртывание на выбранном хостинге.
