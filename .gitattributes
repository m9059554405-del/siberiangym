# Shell-скрипты выполняются внутри Linux-контейнеров (Alpine) — CRLF в
# начале файла (например из-за автонормализации Git на Windows) ломает
# шебанг и выполнение. Форсируем LF независимо от локальных настроек.
*.sh text eol=lf
